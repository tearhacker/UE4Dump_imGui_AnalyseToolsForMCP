# UMT 设备端 API 接口文档 · 总索引

> **定位**：全栈类比里的「后端 API 文档」。
> **方法**：逐模块精读源码 → 记录精确签名、返回语义、边界条件、陷阱。
> **原则**：写不出来的接口宁可标"待补"，也不许凭描述编造。
>
> 上级文档：《泪心UE4_设备端API接口清单.md》（八层总览）
> 下游文档：《泪心UE4_MCP工具功能规格.md》（最终暴露给 AI 的 42 个工具）

---

## 分层与模块

```
L0 会话层      12-executable（进程枚举 / 探针失效 / UI 状态机）
L1 原语层      01-UEMemory · 14-KittyMemoryEx
L2 配置层      02-UEGameProfile · 04-UEOffsets
L3 对象层      03-UEWrappers ✅
L4 分析层      05-AutoFix主流程 · 06-结构布局 · 07-属性偏移 · 08-函数解析 · 09-Arm64解码
L5 批处理层    10-Dumper · 11-UPackageGenerator
L6 查询层      13-SDKExplorer
L7 远程调用层  14-KittyMemoryEx（ptrace 部分）
```

---

## 模块清单

| # | 文档 | 源码 | 规模 | 状态 |
|---|---|---|---|---|
| 00 | 本索引 | — | — | ✅ |
| 01 | `01-UEMemory-内存原语.md` | `src/UE/UEMemory.{hpp,cpp}` | 154 + 352 | ⬜ 待写 |
| 02 | `02-UEGameProfile-引擎定位.md` | `src/UE/UEGameProfile.{hpp,cpp}` + `UEGameProfiles/` | 77 + 1336 + 8 个 profile | ⬜ 待写（**优先级最高**，定位入口） |
| 03 | `03-UEWrappers-对象层.md` | `src/UE/UEWrappers.{hpp,cpp}` | 978 + 1651 | 🟡 **已写**，已精读 1–300 / 320–575 / 735–975，余 `:298-320` `:592-729` `:975-1651` 待补 |
| 04 | `04-UEOffsets-偏移表.md` | `src/UE/UEOffsets.{hpp,cpp}` | 315 + 826 | ⬜ 待写（9 档版本偏移表的完整字段） |
| 05 | `05-AutoFix-主流程.md` | `src/AutoFix/AutoFixOffsets.cpp` | **3185** | ⬜ 待写（最大，建议再拆 2–3 份） |
| 06 | `06-AutoFix-结构布局.md` | `src/AutoFix/StructLayout.cpp` | 630 | ⬜ 待写 |
| 07 | `07-AutoFix-属性偏移.md` | `src/AutoFix/PropertyOffsetFinder.cpp` | 483 | ⬜ 待写（13 项子属性） |
| 08 | `08-AutoFix-函数解析.md` | `VirtualFunctionResolver.cpp` + `NativeFunctionResolver.cpp` | 239 + 377 | ⬜ 待写 |
| 09 | `09-AutoFix-Arm64解码.md` | `src/AutoFix/Arm64Decode.cpp` | 117 | ⬜ 待写（签名已拿到，最快） |
| 10 | `10-Dumper-探测与转储.md` | `src/Dumper.{hpp,cpp}` | 59 + 560 | ⬜ 待写 |
| 11 | `11-UPackageGenerator-产物生成.md` | `src/UPackageGenerator.{hpp,cpp}` | 84 + 678 | ⬜ 待写（10 类产物格式） |
| 12 | `12-executable-会话与编排.md` | `src/executable.cpp` | 1823 | ⬜ 待写（外迁清单在此） |
| 13 | `13-SDKExplorer-查询视图.md` | `src/SDKExplorer.cpp` | 1455 | ⬜ 待写（容器视图 / 运行时视图） |
| 14 | `14-KittyMemoryEx-底层库.md` | `KittyMemoryEx/*.{hpp,cpp}` | 约 6000 | ⬜ 待写（只写用到的：Scanner / Trace / PtrValidator / MemOp） |

---

## 每份文档的统一格式

```markdown
# L? 层名 · 模块名
> 源码路径与规模
> 已精读范围（未覆盖的在文末列出）

## 0. 全局约定        ← 读代码前必知的宏、缓存机制、空值语义
## 1..N. 各接口       ← 签名 | 行为 | 边界条件 | 行号
## N+1. MCP 封装陷阱清单
## N+2. 本次未精读范围
```

每个接口必须回答四个问题：
1. **签名是什么**（照抄源码，不改写）
2. **失败时返回什么**（`"None"` 还是 `""`？`nullptr` 还是 `{}`？`0` 还是 `-1`？）
3. **边界条件是什么**（越界、偏移为 0、未初始化）
4. **有什么陷阱**（性能、误用、版本差异）

---

## 已发现的跨模块约定

### 空值语义不统一（必须逐接口确认）

| 场景 | 返回 | 出处 |
|---|---|---|
| FName 解析失败 | `"None"`（**非空串**） | `UEWrappers.cpp:150,158,161` |
| UObject 属性失败 | `""` | `UEWrappers.cpp:223,231,245` |
| `UE_UObject::GetIndex()` | `-1` | `:190` |
| `UE_UObject::GetFlags()` | `NoFlags` | `:183` |
| 偏移表项为 0 | `0` / 空对象 | `:531-532,742-743,753-754` |

### 偏移为 0 = 该版本不支持

`getter` 普遍写成 `offset ? vm_rpm_ptr<T>(object + offset) : 0`。
**偏移表里为 0 的项表示"当前 UE 版本无此字段"，不是"偏移是 0"。**

### `UE_STATIC_CLASS_CACHED` 按代数缓存

所有 `StaticClass()` 惰性查找并按 `gInitGen` 缓存。
`UEWrappers::Init()` 递增代数 → **切进程必须重新 Init**。

### 全局单例，非线程安全

`UEWrappers::GUVars` / `pObjectsArray` 是文件级全局。
**命令服务必须串行调用本层接口。**

---

## 推进顺序建议

按「依赖 + 性价比」排，不是按编号：

| 序 | 模块 | 理由 |
|---|---|---|
| 1 | **02-UEGameProfile** | 定位入口，MCP 的 `locateEngineGlobals` 直接依赖；且今天真机失败就卡在这 |
| 2 | **09-Arm64解码** | 仅 117 行，签名已拿到，几十分钟可完成，用来验证文档粒度 |
| 3 | **06-结构布局** | 630 行，AutoFix 里最独立、最可复用 |
| 4 | **08-函数解析** | 616 行，汇编解析最易错，最需要严谨 |
| 5 | **07-属性偏移** | 483 行，13 项子属性 |
| 6 | 01 / 04 / 10 / 11 | 原语、偏移表、转储、产物 |
| 7 | 12 / 13 | 外迁清单与查询视图 |
| 8 | 05-AutoFix主流程 | 3185 行，最后做，建议再拆 |
| 9 | 14-KittyMemoryEx | 只写用到的四个类 |

---

## 质量红线

写每份文档时，**以下情况必须显式标注而不是猜测**：

- 未读到的实现 → 「本次未精读范围」列出行号区间
- 语义不确定的参数 → 标注 `（语义待确认）`
- 疑似源码 bug → 标注 `⚠️ 疑似 bug` 并说明依据
- 版本相关行为 → 标注适用版本

**宁可留白，不可编造。** 编造的接口做出来的 MCP 工具必然是错的。
