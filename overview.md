# 两端 MCP 工具成熟度审查结论

## 总结

已完成 Android/C++ 设备端与 Python MCP 服务端的源码、工具注册、参数契约、通信协议、测试及构建审查。

结论：**两端功能覆盖已经很强，43 条设备命令与 43 个 MCP 工具完整对齐，当前可视为高级开发版/内部可用版；但还不能客观称为“特别完善”或生产级完全收口。**

## 已确认完善的部分

- C++ 设备端实际注册 43 条命令，Python 端实际挂载 43 个 FastMCP 工具。
- Python `self_check()` 会检查命令覆盖、命令名映射和设备端参数键，当前通过。
- HELLO → AUTH → auth_ok、一次性 token、NDJSON 分帧、心跳、错误分层、重连和严格串行架构已实现。
- 设备端只监听 `127.0.0.1:27185`，通过 `adb forward` 暴露；未认证连接不能执行命令。
- 危险写内存、远程调用、scratch 分配具备显式确认门。
- C++ 项目已用 NDK 27 完成全量干净构建：57 个目标全部编译并成功链接。
- Python 测试结果：28 passed，14 skipped。

## 主要阻断问题

1. **14 个真实 socket 传输测试被整体跳过。** 错误 token 用例的实际问题已定位到连接重试语义：`connect()` 默认看似只试一次，但配置 `RECONNECT_MAX_ATTEMPTS=0` 被解释为无限重试；不可重试的 `E_BAD_TOKEN` 也会继续重连，mock 设备只接受一次连接，随后表现为握手超时/连接拒绝。
2. **PC 请求超时后连接没有立即废弃。** 设备端迟到响应可能残留，下一请求会读到错误 ID 并报协议错误。超时后应断开重连，或实现按 ID 隔离/丢弃迟到响应。
3. **C++ `isFast` 标志只写不读。** 所有 handler 仍由主线程同步执行；慢命令可能冻结 ImGui。设备端 socket 线程仍会发送心跳，因此不会被误判为假死。
4. **120 秒“硬超时”不能中断正在执行的 C++ handler。** socket 层会返回 E_TIMEOUT，但主线程 handler 仍可能继续执行并产生孤儿响应。
5. **Python 读缓冲区缺少无换行累计上限。** 单帧完成后才检查 1MB；恶意或异常对端持续发送无换行数据时，buffer 可持续增长。
6. **资源和高级协议能力未完全落地。** `umt://process` 仍是占位；动态 capabilities 未按 ptrace 可用性裁剪；START_PROBE/START_DUMP 未统一接入 jobId；cursor 分页、流式分块、adb pull 大文件通道未完成。
7. **工具 schema 主要只有基础类型。** 地址格式、枚举、数值上下限等大量约束仍靠文档和设备端运行时检查，没有完整落到 JSON Schema。

## 成熟度评分

- C++ 设备端功能覆盖：8.5/10
- C++ 设备端运行时健壮性：6.5/10
- Python MCP 工具契约：8.5/10
- Python 传输层验证：5.5/10
- 两端综合成熟度：约 7/10

## 建议优先级

1. 修正 bridge 不可重试错误与默认重试逻辑，解除 14 个 socket 测试的 skip。
2. 请求超时或响应 ID 错配时立即 teardown，防止迟到响应污染下一请求。
3. 给 Python reader buffer 增加无换行累计上限。
4. 真正接通 C++ 快/重命令调度，或将所有慢任务改为 job 化异步执行。
5. 完善 process 资源、动态 capabilities、统一 jobId/cancel、分页/大文件通道。
6. 最后进行真机端到端验证，包括 ping、进程选择、attach、内存读取、probe、dump、cancel 与 ptrace 危险操作。
