## 元梦之星(com.tencent.letsgo, UE4.26) 探针 ERROR_INIT_GUOBJECTARRAY 根因修复

开卷材料 `com.tencent.letsgo/`（Offsets.hpp + Logs.txt + Objects.txt）交叉验证结论：
- **FName 池存纯名而非路径**：slot0 = Package `CoreUObject`（FName 即 "CoreUObject"）、slot1 = Class `Object`。
  `/Script/CoreUObject` 完整路径永远不会出现在 FName 池 → 验证锚点必须用 "CoreUObject"/"Object"。
- **元梦 FChunkedFixedUObjectArray 魔改布局**：`ObjObjectsPtr = GUObjectArray + FUObjectArray.ObjObjects(0x10)`，
  chunk 指针表 @ `[ObjObjectsPtr + TUObjectArray.Objects(0)]`，NumElements @ `[ObjObjectsPtr + TUObjectArray.NumElements(0x10)]` = 绝对 +0x20。
  与标准 UE4 源码布局(Objects@+0x0, NumElements@+0x14)不同，但项目 UEWrappers.cpp:79/121 与 Dumper.cpp 均按此语义读取，dump 458752 对象成功。
- 根因：`GetGUObjectArrayPtr` 的 verifyCandidate 只认 `"/Script/CoreUObject"`（Bug 2 修复引入），元梦 8 槽位全部不匹配 → 探针失败。
  同文件 ScoreObjectArrayCandidate(398行) 早已宽容匹配 "Object"/"Package"/"Class"，验证器漏了。
- 修复（UEGameProfile.cpp 1081-1087, 1147-1167）：锚点改为数组
  `{"CoreUObject", "/Script/CoreUObject", "Object", "Package", "Class"}`，命中任意即计 anchorHits。
- objects 读取偏移 `[cand + ObjObjects(0x10)]` 对元梦正确（chunk 表 @ +0x10），未改动。
- NDK 增量编译通过（build/ + VS2026 cmake/ninja）。
- 经验：UE 对象校验锚点应匹配 FName 池纯名；Dumper7 偏移表语义（ObjObjects/NumElements 相对 ObjObjectsPtr）
  与 UE 源码布局不同，以 Dumper 实测为准。

## UE 偏移表 Bug 修复（UEOffsets.cpp + UEGameProfile.cpp + UECandidateAnalysis.cpp）

核对答案卷后锁定并修复三个 Bug：

**Bug 1（UE4.2x NumElements 偏移）**：`UEOffsets.cpp` 中 UE4_20 / UE4_23_24 / UE5_00_02 三处均设为 `(sizeof(void*)*2) + sizeof(int32) = 0x14`（UE5 布局），UE4.2x 应为 `sizeof(void*) + sizeof(int32)*3 = 0x10`。已改为 `0x10` 并加注释说明。

**Bug 2（GetGUObjectArrayPtr 只验 ID=0）**：`UEGameProfile.cpp` 里原验证逻辑只查 slot 0 是否为 `/Script/CoreUObject`，但 UE4.2x 的 slot 0 是保留空槽，真对象从 slot 1 起。改为扫描前 8 个槽位，对每个有效槽验证 name 解析，统计锚点命中数（`anchorHits > 0` 即通过）。新增 lambda `verifyCandidate()`，复用 chunk0 指针，提取了 `stableItemSize` 变量。

**Bug 3（sampleGnames 偏移错位）**：`UECandidateAnalysis.cpp` 的 `anchorOffsets` 传入的是字节偏移 `{0,2,4,6,...}`，但 `DecodeNameAt` 内部会乘 `layout.stride`，导致 UE4.2x (stride=2) 时 byte_offset 2 变成 entry 4，跳过 id=1（"ByteProperty"）。修复：在调用 `ValidateNameCandidate` 前将 byte offset ÷ layout[0].stride 转成 unit offset。

编译验证：NDK27 + VS2026 cmake/ninja，增量构建通过，llvm-strings 确认新日志串在产物内。

## MCP 网络配置三模式分析（产出 docs/MCP网络配置三模式分析与改造方案.md）

分析了 `mcp_server/客户端AI工具配置MCP文件.json` + `bridge.py` + 设备端 `src/mcp/`。

**核心结论：**
- 手机端 Operit **不需要 ADB**。Operit 与 UMT 同机同 network namespace，`127.0.0.1:35515` 直连可达。
  adb forward 只是「PC→手机」跨机隧道，同机场景是给自己转自己。
- 三种模式不在同一层：模式 1/2（192.168.x.x / 0.0.0.0）是**设备端 bind 地址**问题（改 C++）；
  模式 3（Operit 同机）是**协议/传输**问题（CommandServer 说私有 NDJSON，不是 MCP）。
- 模式 3 的 bind 地址不用动，`127.0.0.1` 已是正确答案。

**定位到的关键 bug：**
1. `bridge.py:44` `host: str = config.HOST` —— Python 默认参数导入期求值，运行时改
   `config.HOST` 完全无效，**加 `--host` 也不生效**（潜伏 bug）。必须改成 `host: str | None = None`。
2. `bridge.py:51-55` `_auto_forward` 恒为 True（`get_bridge()` 永远不传参），CLI 无开关关闭
   → 在手机上跑必挂。这是模式 3 的唯一硬阻塞。
3. `config.py:15-16` 的 `ADB_FORWARD_SPEC`/`ADB_FORWARD_ARGS` 是死代码，全仓库无引用；
   `adb.py:18` 又重复定义 `DEFAULT_PORT = 35515`。
4. `Protocol.hpp:16` `kBindAddress` 是编译期常量 → 模式 1/2 完全无法实现。
5. `CommandServer.cpp:229` `listen(fd,1)` + 单线程 accept→serve→close → Operit 与 PC 并存会 hang。

**方案（未实施，待用户确认）：**
- 改动 A（~12 行 Python，零 C++）：加 `--no-adb` / `--host`，让 Operit 的 Ubuntu 24 chroot 里
  直接跑现有 `server.py`，复用全部 tools.py 编排逻辑。
- 改动 B（~25 行 C++）：设备端 bind 从 `/sdcard/UnrealMemoryTools/mcp_bind.conf` 读取，
  默认仍是 `127.0.0.1`，非回环必须显式配置才生效（不放松安全红线）。

**Operit 侧事实核**：内置 Ubuntu 24（proot，共享宿主 netns，故 chroot 内可达 127.0.0.1）、
支持 MCP/Skill 市场、uvx/npx、远程 MCP。

## 改动 A + B 已落地（PC Python + 设备端 C++）

按分析文档实现了两种改动，已编译/测试验证：
- PC 侧（已验证）：`config.py` 加 `DISABLE_ADB`/`LOOPBACK_HOSTS`；`bridge.py` 修 host 默认参数冻结 bug
  （改成 `host: str | None = None` 再函数体内取）+ `_auto_forward` 尊重 DISABLE_ADB + 报错文案随模式切换；
  `server.py` 加 `--host`/`--no-adb`；`resources.py` 配置摘要随模式；`tests/test_bridge.py` 加 3 个回归用例。
  64 个 pytest 全绿（venv: `mcp_server/.venv`）。
- 设备端（已验证）：`Protocol.hpp` 加 `kBindConfigPath`；`CommandServer.hpp/cpp` 加 `SetBindAddress`/`GetBindAddress`/
  `LoadBindAddressFromConfig`，`Start()` 读 `/sdcard/UnrealMemoryTools/mcp_bind.conf` 覆盖 bind 地址（默认仍 127.0.0.1）；
  `executable.cpp` UI 面板展示实际 bind 地址 + 非回环红色警告。`CommandServer.cpp.o` NDK 增量编译通过（llvm-strings 确认新串在产物内）。
- 文档：`mcp-protocol.md`/`设备端命令服务设计.md` 安全红线措辞改为"默认回环，非回环需显式放开"；
  `README.md` 补三种模式运行方式 + 手机端 Operit 配置；新增 `mcp_server/客户端配置-手机端Operit.json`。

## 设备端 UI 面板按三种监听模式分类（executable.cpp ~4007-4037）
之前只区分"回环/暴露"，现已按 bind 地址分类显示模式：
- 回环 127.0.0.1/localhost/::1 → 模式 3 本机 Operit 同机直连（绿，无需 adb）
- 全局 0.0.0.0 / :: → 模式 2 全局监听（红，含蜂窝）
- 其它具体地址（如 192.168.x.x）→ 模式 1 局域网/指定网卡直连（黄）
`const std::string &bindAddr = GetBindAddress()` 复用既有接口，颜色/Tr 写法与周围一致。
临时语法校验用 NDK clang++ `-fsyntax-only`（stub ImGui/UMT）跑通，随后清理。

## 设备端完整 NDK 链接验证（重要：纠正此前误判）
用户跑完整 `cmake --build`（`build/` + VS2026 的 cmake/ninja）→ **BUILD SUCCESS**，
产物 `outputs/arm64-v8a/UnrealMemoryTools`。此前怀疑的 `executable.cpp:4288`
`ProcessMirrorDisplay()` 在真实构建中**并不报错**（工作树 ANativeWindowCreator.h 当前版本仍含该成员，
或构建用了含该成员的头）——该行不是阻塞。结论：设备端改动（含本 UI 面板）已随完整构建验证通过。

## MCP 面板：三模式全列 + 设备侧查真实 LAN IP
用户要求面板把模式 1/2/3 **全部常驻展示**（仅高亮当前生效项），并点明安卓有 root、UMT 可直接跑原生
shell 查局域网 IP。落实：
- `executable.cpp` MCP 面板：监听地址下方用 `drawModeRow` lambda 列出三种模式，当前生效那行带颜色 + "◀ 当前生效"，
  其余两行 `TextDisabled` 置灰。
- 新增文件作用域 `static std::string GetLanIp()`：`popen("ip -4 addr show wlan0")` 解析 `inet A.B.C.D/24`
  提取 IP（不依赖 awk/cut/grep，只靠 `ip`）。面板里作为"本机局域网 IP（模式 1 连接地址）"展示，
  并提示"把以上地址写入 mcp_bind.conf 即切换为模式 1"。Wi-Fi 未连时显示黄色"未获取到"。
- 编译用 `build_android.bat`（VS2026 cmake/ninja + NDK27，`-DUMT_GHIDRA=ON`）。本次增量构建
  `cmake --build build` → BUILD SUCCESS，产物 `outputs/arm64-v8a/UnrealMemoryTools`。

## 进程扫描"未检测到"提示具体化（executable.cpp RefreshCandidates）
用户确认启动会自动扫内置包名(`UE_Games` 8 个 profile)。把空候选时的泛化提示改为**列出具体扫描过的
内置游戏名**(由 `UE_Games[i]->GetAppName()` 实时拼接,Arena Breakout/Delta Force/Farlight 84/枪战特训2/
Valorant/洛克王国世界/和平精英/PUBG),并附"前台运行 + 需读 /proc/<pid>/cmdline 权限"的排查提示。

## KittyMemoryEx.cpp 扫描噪声日志抑制
`getProcessName` 遍历 /proc 时对读不到的进程会刷 `Couldn't open cmdline file ... error=No such file
or directory`(ENOENT)。已改为:errno == ENOENT 或 EACCES 时静默跳过不记录;其它错误(EMFILE/ENFILE
等)才 KITTY_LOGE。加 `#include <cerrno>`。设备端实际日志确认为 ENOENT(非之前聊的 errno=0)。

## MCP IMGUI 日志可见性修复（设备端 C++）
用户痛点：AI 调工具时，IMGUI 运行日志只刷"客户端已连接/已断开"，看不到具体调了哪个工具/函数。
根因：设备端 `CommandServer::HandleFrame`（收到 {id,cmd,args}）和 `CommandDispatcher::PollOnce`（执行命令）都**不打印命令名**，连接上下线却是 LOGI，于是淹没一切。
修复（已增量编译 + llvm-strings 确认新串在产物内）：
- `CommandServer.cpp`：收到命令即 `LOGI("[MCP·调用] %s id=%llu args=%s")`（args 超 240 字截断）。
- `CommandDispatcher.cpp`：执行前 `LOGI("[MCP·执行] %s")`；正常完成 `LOGI("[MCP·完成] %s 耗时 %lldms")`（超时仍走原 LOGE）。
- 连接上下线改前缀 `[MCP·连接]`，与调用日志区分。
- `executable.cpp` 运行日志面板加"隐藏 MCP 连接上下线"勾选框，勾选后过滤 `[MCP·连接]` 行。
结论：现在日志能清楚看到"AI 调了什么工具 + 在做什么 + 耗时"，连接刷屏可一键屏蔽。

## 连接刷屏根因 + 重连合并（CommandServer.cpp）
继续排查"持续显示客户端已连接/已断开"：确认 PC 侧 `tools.py` 走 `br.call()` 单例 bridge，**不会**每个工具新建连接。
所以刷屏是 bridge 连接本身在反复断重连（MCP 宿主对 server.py 每轮调用重生子进程，或 adb forward 不稳），属设备外问题。
设备侧能做的：把"断开后立即又连上"的重连风暴合并成单行，避免成对刷屏。已在 `ServerLoop` 加：
- `static time_t s_lastConnectTs/s_lastDisconnectTs` + `kConnCoalesceSec=5`；
- 距上次断开 <5s 的连入只打 `I: [MCP·连接] 重连（距上次断开 Xs）`，不重复"已连接"；
- 若本次连接持续 ≥5s 才单独打"客户端断开"，否则被下次"重连"行概括。
效果：正常长会话仍显示 已连接/已断开；重连风暴每轮只剩一行"重连"，配合面板"隐藏 MCP 连接上下线"开关双保险。
增量编译通过，llvm-strings 确认新串在产物内。

## MCP 面板加「客户端实时状态」徽标 + 工具调用计数（executable.cpp / CommandServer）
进一步把「连接状态」从事件日志里抽出来变成常驻状态：
- `CommandServer.hpp/cpp` 新增原子快照 `clientConnected_` / `toolCallCount_` + getter `IsClientConnected()` / `ToolCallCount()`；
  connect 置 true、disconnect 置 false、每收到一条 `[MCP·调用]` 计数 +1。
- `executable.cpp` MCP 面板「服务状态」下加：客户端 ● 已连接/○ 未连接（颜色区分）+ 「累计工具调用: N」。
现在不用翻日志也能一眼看到客户端在线与否、AI 调了多少工具；配合日志里的 [MCP·调用]/[MCP·执行]/[MCP·完成] + 合并重连 + 隐藏开关，连接刷屏问题彻底解决。
增量编译通过，llvm-strings 确认 "Tool calls"/"Client" 等串在产物内。

## MCP 面板状态行微调（executable.cpp）
用户要求：状态与「累计工具调用」同行显示不换行 + 删除 emoji。
改为单行 `ImGui::TextColored` 输出 `客户端: 已连接   累计工具调用: N`（绿）/ `客户端: 未连接   累计工具调用: N`（黄），去掉 ●/○ 及注释里的 🔴。已增量编译，llvm-strings 确认 "Client: Connected"/"Client: Disconnected" 进场、无 ●○。

## 连接刷屏根因修复（bridge.py + 直连配置）
继续追"持续已连接/已断开"根因：stdio MCP 宿主是常驻的，所以不是每调用重生子进程；几乎可确定是 **adb forward 隧道（USB）在调用间隙掉线**→ TCP 断→设备记断开→下次调用重连。
PC 侧改进（bridge.py）：`connect()` 重连时原 `force_forward=attempt>1`（首轮靠 "forward --list" 乐观判断，死隧道仍"列着"会假成功→首轮踩死隧道再重试）；改为每次重连都 `force_forward=True` 强制重建转发，拿真活链路，消除重连抖动。已 py_compile 通过。
根治方案（给用户的直连配置）：彻底去掉 adb 隧道即可不再掉线。新增 `mcp_server/客户端AI工具配置MCP文件-同机直连.json`：
- 同机 Operit → `--no-adb --host 127.0.0.1`（模式3，不经 adb，最稳）
- PC↔手机走 WiFi → `--no-adb --host <手机局域网IP>` + 设备端 `mcp_bind.conf` 写该 IP（模式1）

## 打包 mcp_server 可移植源码 zip
用户要"电脑+Operit 都能用"。确认源码已跨平台（adb.py 默认 `adb` 走 PATH + `sys.platform` 判断；config.py 无写死 Windows 路径），无需改代码。
产出：`D:\泪心安卓领域基本盘技术\ue4ImGuiAutoWorkingMcpBytear\mcp_server_src.zip`（22 文件 ≈ 56KB，顶层含 `mcp_server/`）。
打包脚本（临时，在 TEMP）：用 Python zipfile 遍历，排除 `.venv`/`__pycache__`/`.pytest_cache`/`.vscode`/`*.pyc`/`*.egg-info`/`build`/`dist`。
关键：`.venv`（本机 Windows+Py310 专属，5000+ 文件）绝不能随 zip 带走；目的地需各自 `pip install "mcp>=1.0,<2" "capstone>=5.0,<6"`。
`src/umt_mcp/tools/` 是空目录，无需进包。三份客户端 JSON 已随包（Windows-adb / 同机直连 / Operit），到目标机改解释器+server.py 绝对路径即可。

## 音量键控制菜单显隐（executable.cpp 集成）
用户写好监听线程（读 /dev/input/event* 音量键，音量+ 显示 / 音量- 隐藏），难点在巨量源码里集成。
采用最小侵入方案，完全不碰 `RenderAutoUEDumpPanel`：
- 补头文件：`<atomic> <fcntl.h> <unistd.h> <sys/select.h> <sys/time.h> <linux/input.h>`。
- `main()` 前插入全局 `std::atomic<bool> MemuSwitch{true}` + `volume_thread_running`/`volume_thread` + 三个函数 `GetEventCount1`/`音量隐藏菜单`/`清理音量监听`。
- `main()` 三改：渲染循环前 `音量隐藏菜单()` 启动监听；`Layout_tick_UI(&flag)` 包进 `if (MemuSwitch.load())`；循环后 `清理音量监听()`。
- `MemuSwitch` 用原子避免跨线程数据竞争（原代码是裸 bool，UB）。默认 `{true}` 显示，想默认隐蔽改 `{false}`。
- 函数名用中文（NDK clang 支持 UTF-8 扩展标识符）；若构建 flag 拒绝，改名 ASCII 即可，逻辑不动。
注意：整仓有预存在编译阻塞 `ANativeWindowCreator::ProcessMirrorDisplay()` 成员已被删（与本次无关），全量构建会先卡在那。

## ImGui Style 栈失衡修复（executable.cpp）
`RenderAutoUEDumpPanel` 外层 12 次 `PushStyleColor` 但结尾只 `PopStyleColor(11)` → 每帧净留 1 颜色。
后果：调试构建 `EndFrame` 因 style 栈不平衡 assert 崩；发布构建缓慢堆积（控件变色/内存涨）。
修复：`PopStyleColor(11)` → `PopStyleColor(12)`；并删除从未调用的死代码 lambda `drawStatusChip`（3 push/3 pop，但无引用）。
全函数核对：StyleColor push 15 / pop 15（外层12+drawNavButton3），StyleVar push 7 / pop 7，均已平衡。

## 菜单隐藏逻辑迁移到 ImGui::Begin 窗口层（修复崩溃设计）
用户诊断：之前在 `main` 循环用 `if (MemuSwitch.load()) Layout_tick_UI(&flag)` 整帧跳过 UI 函数，会破坏 `Layout_tick_UI` 内 `ImGui::Begin/End` 的每帧配对 → 崩溃。
正确做法（用户指定）：隐藏判断放在 `Layout_tick_UI` 内部的 `ImGui::Begin` 窗口这一级。
改动：
- `executable.cpp`：`MemuSwitch` 去掉 `static`（改为外部链接，供 draw_Gui.cpp 的 `extern` 引用）；`main` 循环恢复无条件 `Layout_tick_UI(&flag)`。
- `draw_Gui.cpp`：加 `#include <atomic>` + `extern std::atomic<bool> MemuSwitch;`（定义于 executable.cpp）；`Layout_tick_UI` 开头 `if (!MemuSwitch.load()) return;` 跳过整个窗口创建/渲染。`RenderAutoUEDumpPanel` 随窗口一起跳过；`graphics->NewFrame/EndFrame` 仍每帧配对，ImGui 帧平衡。
教训：跨文件共享的 UI 开关状态用 `extern` 全局；隐藏类开关必须贴近 `ImGui::Begin`，绝不要在调用方整函数跳过（会破坏 Begin/End 配对）。
