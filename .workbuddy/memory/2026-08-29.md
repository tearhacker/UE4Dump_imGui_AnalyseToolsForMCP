# 2026-08-29

## 通读 源项目（ZYGISK IL2CPP MCP v2.0.0）

目录：`D:\泪心安卓领域基本盘技术\ue4ImGuiAutoWorkingMcpBytear\源项目`

- 这是一个 Magisk/KernelSU 模块（module.prop: id=zygisk_il2cpp_mcp, versionCode=200, minMagisk=20400, author=洋葱落日），
  通过 Zygisk 注入 .so 到目标进程（默认包名 com.pi.czrxdfirst），在进程内起 TCP 命令服务，默认端口 27184。
- PC 侧 `mcp_server.py`（1978 行，纯标准库 stdio MCP server，62 个工具）经 `adb forward tcp:27184` 连接。
- 原生协议：发一行命令，回 `OK <len>\n<body>` 或 `ERR <msg>`；body 多为 JSON。
- 编码约定：字符串参数一律 UTF-8 hex 传输（`_hex_text`）；地址用 `int(x,0)` 解析后以 hex 下发；
  内存类型值用 struct 小端 pack（MEMORY_VALUE_FORMATS）。
- 工具分组：连接/UI 12、IL2CPP 10、Memory 16、Dobby 9、Lua 5、Assembly 4、Breakpoint 6。
- 配置持久化在 `/data/adb/zygisk_il2cpp_mcp/`（apps.txt / port.txt / memory_backend.txt / driver_node.txt），
  由 customize.sh 首次安装时创建并从旧版本迁移，升级不覆盖。
- webroot/ 是模块内置 WebUI（用于选内存后端）；mcp.zip 是 PC 侧打包（mcp/ 目录下 4 个文件）。

### 待办/观察
- `test_mcp_server.py` 里 `from mcp.mcp_server import ...` 与当前目录结构不匹配，需放到 mcp/ 包下才能跑。
- `run_stdio` 单线程串行，长调用（wait_input 最长 300s、memory_search 下限 300s）会阻塞整个 MCP 通道。
- 每个工具调用前会额外发一次 `UI_TOAST_NOTIFY`，等于每次调用多一个 socket 往返（排除集合见 `_notify_mcp_call`）。

## 生成 源项目/readme.md（快速上手文档）

- 用户提供了上游官方 README 全文，已写入 `源项目/readme.md` 作为权威部分。
- 我在此基础上追加「代码级补充」章节：调用链路、原生协议格式（OK len / ERR）、三条编码约定
  （hex 字符串 / 地址归一化 / struct 小端）、62 工具分组表、实现细节、5 条已知坑。
- 上游路径是 `mcp/mcp_server.py` + `mcp/README.md`；本目录平铺，已在文档中用引用块标注差异。

### 上游官方补充事实（代码里看不到，来自官方 README）
- 内核内存后端可选：System / KPM KMA / ditPro / APRead / ioctl hook / Netlink / GT1 / GT2 / Paradise / QX。
- Zygisk Root companion 读配置后经 IPC 传给目标进程（兼容应用进程无法访问 /data/adb）。
- apps.txt 支持多包名，且同时匹配主进程与 `包名:子进程`；改配置后必须彻底杀死并重启目标 App。
- 注入成功后目标进程会 Toast 显示 `TG: @il2cppmcp`，Android 上下文未就绪时会短暂重试。
- 默认模式行为是「先直连，失败后才 adb forward」，`--direct` 关闭该回退（与代码 120-129 行一致）。
- 代码 Patch 不会被 DobbyDestroy 自动恢复。

## 真实目的：给 Andriod_UnrealMemoryTools 做自己的 MCP（可行性分析）

用户真实意图：为自己的 UE4 Dumper 项目也开发一套 MCP 工具。结论：**可行，且比 Zygisk 方案简单**（自身即独立进程，不需注入/hook）。

### 项目性质（关键）
- 外部 Dumper，**不注入不 hook**，纯 `/proc/<pid>/mem` 读内存；独立 ELF 二进制，Vulkan + ImGui 悬浮窗。
- 需要 root。构建：NDK r25+ / CMake + Ninja / clang C++20，产物 `outputs/arm64-v8a/`。
- 输出目录 `/sdcard/UnrealMemoryTools/<package>/`（Logs.txt、Objects.txt、Offsets.hpp、SDK_*.hpp、script.json、libUE4.so）。

### 已具备的 MCP 基础设施（executable.cpp）
- `DumpUiState` + `std::mutex`（110-135 行）：UI 每帧加锁 poll 的共享状态，命令服务可直接复用此模式。
- `gWorkerThread` + `ExecuteProbe` / `ExecuteDump` / `ExecuteDumpUnrealLib`（945-989 行）：单 worker 跑重活。
- `PushUiLog` / `LoggerSink`（227-242 行）：日志已结构化收在内存。
- API 边界干净：`UEDumper::Init(profile)` / `UEDumper::Dump(buffers)`（Dumper.hpp:31-33）。
- `FindAutoProcessCandidates()`（166 行）枚举进程；`SDKExplorer.cpp`（137-511）有对象检索 + 属性面板。
- `main()` 在 1761 行，`while(flag)` 渲染循环在 1805 行，`Layout_tick_UI(&flag)` 每帧调用。

### 方案设计
- **方案 A（推荐）**：进程内加命令服务线程 → 命令队列（mutex + cv）→ 复用 gWorkerThread 执行 → 状态/结果回写轮询。
  长任务必须异步化（投递回 job_id，另用 job_status 轮询），否则阻塞 MCP 通道（源项目就吃了这个亏）。
  建议端口 27185，避开源项目 27184。
- **方案 B（保底）**：加 `--headless --cmd "..."` 参数，跑完 print 到 stdout 退出，MCP 层用 `adb shell su -c` 调。
  零线程改造最安全，但每次冷启动且无法保持已探测状态。
- 风险：Vulkan/ImGui 悬浮窗依赖屏幕（灭屏 ANativeWindow 可能失效 → 建议 headless 跳过图形初始化）；
  root 权限；adb forward 仅 localhost 生效。

### 下一步
等待用户选择方案后开工；mcp_server.py 可直接复用源项目骨架，删掉 IL2CPP/Dobby/Lua/Asm/断点部分。

## 场景题澄清与边界决策（重要，后续会话沿用）

用户诉求：dump 出来的类名/方法/属性太多看不懂，手动在 ImGui UE4 里一个个找太麻烦，希望 MCP + AI 帮他分析出
想要的内容。并明确提到想让 AI 分析出 ESP 所需数组、人物结构体、阵营等。

**决策**：
- 通用能力方向**全力支持**：读懂 SDK dump、AutoFix 红色字段兜底推理、Profile 健康度巡检、
  基于真实 SDK 写配套工具代码。这些用的是同一套 MCP 工具，只是问法不同。
- **ESP / 人物数组 / 阵营 等具体作弊目标不做**。理由（对用户原话陈述，不讲教）：
  联网多人游戏透视损害其他玩家；国内制售外挂有以非法经营、提供侵入非法控制计算机信息系统
  程序工具罪追诉的判例；用户自己 README 许可条款明确写"禁止用 Dump 出来的 SDK 制作付费外挂"。
- 工具设计原则：做成**通用的 SDK 导航与结构分析器**，不针对任何作弊功能做特化。

### 已给出的技术结论
- AI 的价值是「提候选」，偏移来自 dump 的真实数据，两者不可互换；真正的杀手锏是
  **假设-验证闭环**（AI 提候选 → 工具读真实内存验证 → 返回原始观测值 → AI 修正/收敛）。
- 验证步骤必须返回**观测到的原始值**，不能返回摘要，否则 AI 会在幻觉里自我确认。
- 工程铁律：SDK_Classes.hpp 可达几十 MB，塞不进上下文。工具必须
  「先给计数和索引，再给精确定位内容，永远不整包吐」；过滤下沉到 C++ 侧。
- UE 命名有强约定（A=Actor / U=UObject / F=struct / E=enum / S=Slate / I=interface），
  这是"用类名做语义检索"能成立的前提。

### 待办
用户确认后，产出《UMT MCP 工具规格草案》：15-20 个工具，含输入输出约束与分页规则，审完再写代码。

## AutoFix 失败根因定位（代码级，重要）

用户痛点：某些**单机游戏** AutoFix 搜不出 GNames / UWorld，探测直接失败。已定位到具体硬编码点。

### 致命依赖链
`IGameProfile::GetGUObjectArrayPtr()`（UEGameProfile.cpp:893）第一行
`const uintptr_t namesPtr = GetNamesPtr();`（:900），拿不到就 `return 0`。
→ **GNames 一挂，GUObjectArray 连尝试都不做，GWorld 一并全崩。**

### GetNamesPtr() 的四个硬编码卡点（UEGameProfile.cpp:971 起）
| 卡点 | 位置 | 后果 |
|---|---|---|
| 指针值范围 `0x4FFFFFFFFF~0x7FFFFFFFFF` | :995-996 | 分配器布局不同则一个候选都进不来 |
| FNamePool 锚点偏移 `val + 0x8` | :1030 | 条目头长度变了就永远读不到 |
| GNames 锚点偏移 `p1 + 0x24` | :1046 | 同上 |
| 锚点字符串**只有 "ByteProperty"** | :999-1001 | 该 FName 不在预期位置即判定失败 |

### 白捡的改进点
`UEGameProfile.cpp:80-84` 已有完整多锚点名单
（None/ByteProperty/IntProperty/BoolProperty/FloatProperty/ObjectProperty/NameProperty/
StructProperty/ArrayProperty/Object/Class/Function），
但**只在 `BruteForceDeltaForceNames`（三角洲专用）里用**，通用路径没用上。
→ 把多锚点验证接到通用路径，是**不需要 MCP 的纯 C++ 小改动**，可能立刻救活一批游戏。

### 其他事实
- `DetectVersion()`（AutoFix.cpp:140）：先 `GuessMajorFromSoName()`（libUnreal.so→UE5_03），
  否则扫 rodata 的 `++UE4+Release-X.YY`；**找不到字符串就默认 UE4_25_27**（可能选错偏移表）。
- 扫描范围只在 `ue_elf.segments()` 内找"存在 ELF 里、指向 name pool 的指针"，
  堆上/其他模块的指针扫不到。

### MCP 方案要点
- **分工铁律：扫描必须在设备端 C++ 跑，AI 只负责"换假设 + 判结果"**。
  几百 MB 内存走 adb 给模型看既慢又无意义。
- 需要新增的工具：`ue_detect_version`、`names_scan`（参数化 min/max ptr + 锚点数组 +
  锚点偏移 + 扫描区域）、`names_sample`（返回候选的前 N 个 FName 字符串 ← AI 判断的命脉）、
  `objects_scan`（接受 names 候选 id，解开依赖）、`memory_read*`、`probe_apply`。
- AI 比静态启发式强的地方：启发式只能判"12 字节是否等于 ByteProperty"；
  AI 能判"这 16 个字符串序列像不像 UE 的 FName 表"，信息量与容错都高一个量级。

### 诚实边界（已告知用户）
FName 加密/混淆的游戏（明文锚点失效）AI 也难办；全内存扫描慢，需容忍几十秒；
能显著提高命中率但不能保证 100%（当前硬编码不匹配时是 0%，目标 70-80%）。

## 用户背景补充与自主性设计定案

### 背景
- 源码原作者曦曦(DreamFekk)已停更，用户是在此基础上自己往下做。
- 主要场景：**单机游戏**，包括**用户自己开发的 UE4 手游**。→ 性质属于正当逆向/自研调试。
- 用户诉求：希望 AI 能"产生自己的智慧"——自主搜索、独立判断、跳指针、分析内存，甚至断点调试。

### 定案的设计原则（后续开发必须遵守）
1. **原语 vs 领域工具 = AI 自主性的上限**。
   只给 `find_gnames()` 这类任务工具 → AI 只能做写死的事；
   给足 `read_ptr/read_bytes/scan/list_modules/resolve_symbol/read_maps` → AI 能拼出未预置的策略。
   **四层结构**：AI（顶层循环）→ 领域工具（names_scan/class_search/probe，快路径）
   → 复合操作（follow_chain/scan_candidates，省轮次）→ 原语（AI 的手）。
2. **跳指针是 AI 的强项**：每步解引用后判断"这个值合理吗"（落在哪个模块、像不像数量、
   字符串像不像 UE 名字），这类语义判断正是固定脚本最弱的地方。
3. **三条工程约束**：
   - 轮次预算：纯原语会爆炸（10 层指针 × 5 候选 = 50 次调用），必须用"复合操作"层折中
     （一次调用走 N 层，返回整条链）。
   - 验证纪律：工具必须返回**原始观测值**（十六进制/字符串），绝不返回"看起来对"的总结。
   - 护栏：自主模式**默认只读**；写内存/打断点需显式开关，并用 MemoryBackup 支持撤销。

### 断点调试：需要独立子系统，不能靠 MCP 凭空给
- UMT 是外部进程，断点必须走 ptrace attach（root 下可行）或注入。
- 难点：SIGSTOP 停进程破坏时序、反调试检测；ARM64 硬件断点要走
  PTRACE_GETREGSET / NT_ARM_HW_BREAK，且是**per-thread**，需枚举目标所有线程。
- 结论：**独立立项，工作量可能超过 MCP 桥本身**。建议先做只读分析 + 内存扫描。

### 测试策略（用户优势）
用户自己的 UE4 手游 = **有 ground truth**（真实 GNames/GWorld 地址已知），
可量化 AI 命中率、定位失败发生在哪一步；同时用户掌握确切 UE 版本，
可用来校准 `DetectVersion()` 是否判断错误。这是最理想的测试床，优先用它验证。
（但用户强调：真正要解决的是**第三方单机游戏**，他什么都不知道的情况。）

## 关键定案：自主定位不需要外部特征码（核心设计）

用户质疑："第三方游戏我不知道 GNames/GWorld，怎么给 AI 特征码？"
→ **答案：不需要给。特征码（ADRP+LDR 模式、ByteProperty 字节串）是老办法的思路，
   属于 per-game 适配。自主定位走的是「结构不变式 + 引擎级名字表」，对所有 UE 游戏通用。**

### 三个通用锚（不依赖任何游戏知识）
1. **版本字符串**：`++UE4+Release-X.YY` / `++UE5+Release-X.Y` 在几乎所有 UE 二进制的 rodata 里
   （现有 `ScanVersionString()` 已实现）→ 拿到版本 → 选中偏移表。
2. **引擎级名字表**：FName 表前若干个条目**在所有 UE 游戏里都一样**——
   None / ByteProperty / IntProperty / BoolProperty / FloatProperty / ObjectProperty /
   NameProperty / StructProperty / ArrayProperty / Class / Object / Function / Package。
   这不是"特征码"，是**引擎固有内容**，跨游戏恒定。
3. **结构自洽性**：猜对 GUObjectArray 时，上万个对象会互相交叉验证
   （obj->Class / obj->Outer 都落回同一数组，obj->Name 索引能经 GNames 解出合理字符串）；
   猜错则立刻崩散。**这是自洽检验，零外部知识。**
4. **GWorld 根本不需要单独扫**：一旦 GUObjectArray 可用，
   GWorld = 在对象数组里找 class 为 `World` 的实例（现有 `FindObject` 类接口即可）。

### 漏斗式扫描（速度关键）
全部可读内存(~800MB) → 指针对齐+落点可读筛选(~50万，设备端廉价判断)
→ 结构验证能解析出 FNameEntry(~200，设备端) → **AI 看每个候选的前 16 个字符串样本(PC 端)**。
**昂贵的 AI 步骤永远只接触约 200 份小样本，不碰原始大内存。**
提速手段：先扫最可能区域（UE 模块 .data/.bss）再扫全堆；
置信度达到阈值（如 5 个引擎名全中）早停；候选集按 session 缓存，AI 换判据时不必重扫。

### AI 不可替代的环节（对用户"AI 自己分析"诉求的准确回答）
AI 不是去扫内存，AI 是**能容忍噪声的验证器**。
硬编码打分器要求"12 字节精确等于 ByteProperty"（是非题，容错为零）；
AI 判断的是"这 16 个字符串序列像不像 UE 的 FName 表"（语义整体判断，
即使其中 2 条读错仍可判定）。这是自动化成功率的关键差异。

### 仍然无解的情况（已如实告知）
- FNameEntry 被加密或游戏自定义了名字池 → 结构不变式失效，只能回到代码特征定位
  `FName::GetNames()`，需要反汇编能力，难度大得多（单机游戏里相对少见）。
  **补充：29 个 AndUEDumper profile 里只有 DeltaForce 覆写了 `GetNameEntryString`
  解密钩子 → 加密 FName 实际占比约 3%，不是普遍问题。**
- 二进制被 strip 导致版本字符串丢失 → 退化为"逐个尝试所有版本偏移表"，慢但仍可行。
- libUE4.so 被改名/静态链接 → ELF 探测失败，需改为扫描所有可执行模块找 UE 特征。

## 新素材：AndUEDumper（UMT 的上游）— 重大发现

路径：`D:\泪心安卓领域基本盘技术\ue4ImGuiAutoWorkingMcpBytear\AndUEDumper\AndUEDumper\src\UE`

### 核心发现：pattern 不是"游戏特征码"，是"引擎函数序言"
`UEGameProfiles/Torchlight.hpp` 的注释明写：
`FNameEntry const* FName::GetEntry(FNameEntryId id);`、`DebugDump`、
`GetPlainName ToString AppendString GetStringLength` —— 全是 **UE 引擎函数**。
→ 字节序列由「UE 版本 + 编译器」决定，**跨游戏可迁移，与具体游戏无关**。
佐证：29 个 profile 里 17 个都用 `UE4_25_27` 偏移表。

### 按版本聚合的可用 pattern 数（白捡的资产）
| UE 版本 | pattern 数 | 来源游戏数 |
|---|---|---|
| UE4_25_27 | **211** | 17 |
| UE4_23_24 | 39 | 3 |
| UE5_00_02 | 42 | 3 |
| UE5_03 | 28 | 2 |
（UE4_25_27 是手游最常见的一档）

### UMT 丢了数据但能力还在
- UMT 只保留 6 个 profile（暗区突围/三角洲/远光84/天堂2/夜鸦(NRC)/PUBG）
  + 自研 PUBGMHD/Sfps2/Valorant，**丢了 AndUEDumper 的 23 个**。
- 但机制全在：`findIdaPattern()`（`UEGameProfile.cpp:834`）、
  `Arm64::DecodeADRL()`（`UEMemory.hpp`，且比上游多了 Decode_ADRP_ADD / Decode_ADRP_LDR）、
  `PATTERN_MAP_TYPE`（ANY_R/ANY_X/ANY_W/BSS）、`isEmulator()`。

### 定案：双路径定位（修订原计划）
1. **快路径**：版本检测 → 挑该版本的 pattern 库 → findIdaPattern → DecodeADRL → 候选（秒级）
2. **兜底**：全内存结构扫描（分钟级，不依赖 pattern）
3. **两条路共用同一个验证环节**：解析前 16 个名字看是否引擎名表 → AI 判定
   → 互为交叉校验，误判率大幅下降。

### 修订后的行动顺序（原"先做多锚点"改为先抽 pattern 库）
1. 从 AndUEDumper 抽取 pattern，按版本去重整理成表（纯数据，零风险，收益最大）
2. 多锚点接进通用路径（纯 C++ 小改动）
3. MCP 工具规格草案
4. 断点调试另立项

### 注意
pattern 会随游戏更新失效（这正是 AutoFix 诞生的原因），**不能只靠 pattern**，
必须保留结构扫描兜底。

## 用户追问：上 hook 能否解决那三种无解情况 — 结论

用户设想：给 MCP 加上 Dobby / perf 断点等注入能力，是否就能解决三个难题。
**答案：1 个决定性、1 个间接、1 个无关。**

| 情况 | hook 是否有效 | 机制 |
|---|---|---|
| ① FName 加密/自定义名字池 | **决定性** ✅ | hook `FName::GetEntry(id)` / `FName::ToString()`，让游戏自己解密，
  无需知道加密算法（对比：AndUEDumper 的 `GetNameEntryString` 虚函数要求你"知道怎么解密"）。
  **ptrace 远程调用即可，不需完整注入。** |
| ② 版本字符串被 strip | 间接 ⚠️ | hook 变不出已删除的字符串；但可在进程内调用引擎运行时版本 API，
  或靠访问器绕开"必须先知道版本"的前提。且"挨个试偏移表"本来也能跑。 |
| ③ libUE4.so 改名/静态链接 | **无关** ❌ | 这是探测问题不是访问问题。解法是枚举 /proc/pid/maps 所有可执行模块
  逐个检查 UE 特征（而非按文件名找），纯外部就能做。 |

### hook 的真正价值（比救三个边角重要得多）
**"猜偏移"这件事本身可以被绕过**：有进程内调用能力后，
名字调 `FName::ToString()`、类型判断调 `UObject::IsA()`、属性偏移问 `FProperty::GetOffset()`。
→ **偏移从"前提"降级为"兜底"，是范式变化。**

### 三级能力阶梯（定案）
1. 外部只读（成本低，现在就能做）
2. **ptrace 远程函数调用**（成本中，痕迹小，能解 case ① ← 最容易被忽略但性价比最高）
   attach → 保存寄存器 → 设参数+PC → 继续 → 读返回值 → 恢复 → detach
3. 完整注入 Dobby（成本高，稳定性风险，覆盖全部）
   代价：项目性质改变、稳定性（用户自己另一个项目就在修 4h 长跑稳定性）、
   maps 痕迹、工作量比 MCP 桥本身还大。

### 战略建议（已告知用户）
**不要凭空建注入层**。实测 case ① 占比约 3%，case ③ 手游罕见，case ② 本来就能跑；
真正常见病是硬编码指针范围/单锚点/GNames 依赖链。
→ 先做外部方案 + AI 验证闭环，**拿 10 款游戏实测统计"失败在哪一步"**，用数据决策是否上注入。

## 重大发现：ptrace 远程调用能力已在 KittyMemoryEx 里，且完全可用

**`KittyTraceMgr::callFunctionFrom()`（KittyTrace.cpp:140）是完整正确的远程函数调用实现，
一直在依赖里没人用。** 已逐条核实：

| 能力 | 状态 | 位置 |
|---|---|---|
| ATTACH/DETACH/CONT/WAIT | ✅ | KittyTrace.cpp:28/38/50/71 |
| 寄存器备份恢复（自动选 GETREGSET） | ✅ | getRegs/setRegs |
| ARM64 参数填充 x0–x7 | ✅ | tmp_regs.uregs[i] 循环 |
| >8 参数压远端栈 | ✅ | sp -= ... + Write |
| 返回检测 LR 陷阱 | ✅ | tmp_regs.lr = callerAddress |
| 捕获 SIGSEGV/SIGILL 判返回 | ✅ | WSTOPSIG 判断后 break |
| 区分 WIFEXITED / WIFSIGNALED | ✅ | 有处理 |
| 失败自动恢复寄存器 | ✅ | _autoRestoreRegs |

API：`callFunction(addr, nargs, args...)` / `callFunctionFrom(caller, addr, nargs, ...)`
（`KittyTrace.hpp:114-122`），另有 `setDefaultCaller()` / `setAutoRestoreRegs()`。

### 剩下只需 100-200 行胶水
1. 找函数地址 ← **pattern 库正好干这个（两者是紧耦合上下游）**
2. `FName::ToString(FString&)` 需要可写 scratch 缓冲区（从 maps 挑一块可写区）
3. 读回 FString 结构（data 指针 + 长度）再读字符串本体
4. 接进 probe 流程

### 仍然成立的物理限制
**ptrace 只能"开锁"不能"搬货"**：每次调用要 attach→设寄存器→继续→等信号→取返回值，
进程全程冻结。
- 验证环节（几十次调用）= 毫秒~秒级 → **完美胜任，且与 AI 验证闭环天作之合**
  （AI 判断一个候选只需 16 个名字样本 = 16 次调用）
- 全量 dump（几十万次）= 进程冻结数分钟 → ANR/看门狗，不可行

### 已知缺陷需兜底
SIGSEGV/SIGILL 返回陷阱**无法区分"陷阱触发"与"被调用函数自己崩了"**（信号相同）。
兜底：校验返回值 —— 检查指针可读、字符串是否像 UE 名字（崩了会返回垃圾，一验就露馅，
且这恰好是 AI 擅长的）。

### 冻结风险分析（用户追问"会不会导致闪退"，已答）
| 风险 | 阈值 | 是否杀进程 |
|---|---|---|
| ANR（主线程不响应输入） | 5s | **不自动杀**，只弹系统对话框，可选等待 |
| 游戏自带看门狗（UE 有 hang detector） | 各家不同 | **会 abort**，是真正的闪退来源 |
| 服务器踢线 | 各家不同 | 会断连 —— **单机游戏不存在此风险** |
| 系统杀后台 | 仅后台/息屏 | 与 attach 无关 |

**量化**：attach 开销是"每次 attach 一次"，不是"每次调用一次"。
正确做法 = attach 一次 → 内部连续调 N 次 → detach。
单次调用约 1-5ms，开锁阶段 16 次 ≈ **30-80ms**，距 ANR 的 5s 差两个数量级 → **安全**。
搬货阶段几十万次 → 分钟级 → 必触发看门狗 → 不可行（故搬货必须走 /proc/pid/mem，全程不 attach）。

**比闪退更隐蔽的风险是死锁**：若只挂单个线程让游戏继续跑（ptrace 可指定 tid），
调用 `FName::ToString` 可能撞上游戏持有的锁（FNamePool 有临界区）→ 死锁。
权衡：挂全进程（group-stop，无死锁，但必须短）vs 挂单线程（不冻结，但可能死锁）。
→ KittyTrace 现有实现是 `ptrace(PTRACE_ATTACH, remotePID())` = 挂全进程，**正合用**。

**缓解清单**：单次 attach 内批量调用 / attach 前先快照关键内存 / ptrace 失败回退结构扫描 /
前台操作保持极短。

**最重要的建议（已告知用户）：不要理论，去测。**
用户有自己的 UE4 游戏 → attach 后保持 100ms / 1s / 5s 递增，观察是否闪退，
10 分钟即可测得真实安全窗口。这是最可靠的答案来源。

## 加密 FName 解密所需的 MCP 工具设计（四层）

### 前置事实（已核实）
- **设备上没有反汇编器**：只有 `KittyMemoryEx/Deps/Keystone/` 头文件，
  且 CMake 用 `-DkNO_KEYSTONE` 关掉了。KittyArm64.hpp 无可用公开接口。
- `KittyScanner` 齐全：findBytesAll/First、findHexAll/First、
  **findIdaPatternAll/First**、findDataAll/First。
- → **反汇编放 PC 侧做**（MCP server 里 pip 装 capstone），设备零负担、零体积代价。
  **这是让 AI 能"像人一样读汇编"的关键，也是本设计的点睛之笔。**

### 四层工具
1. **定位层**：`scan_pattern`（IDA 风 pattern）· `resolve_symbol` · `elf_symbols` · `read_memory`
2. **理解层（PC 侧）**：**`disassemble(hex_bytes, base_addr)` ← capstone** · `decode_adrl`（UMT 已有）
3. **准备层**：`scratch_alloc(size)`（ptrace 调目标进程的 mmap 拿可写区）· `memory_write`
4. **调用层**：`attach_begin/end`（会话式，一次冻结内多次调用）
   · `call_function(addr, args[], return_kind)` · `call_function_batch`
   · `read_string(addr, max_len, wide)`（FName 可能是 UTF-16）

### AI 解密 FName 的实际流程
scan_pattern 找 GetEntry/ToString → disassemble 看汇编判断签名（几参、是否 sret）
→ scratch_alloc 备 FString 输出缓冲 → attach_begin
→ call_function × N → attach_end → read_string × N → AI 判"像不像 UE 名字表" → 收敛或换 pattern 重来

### 批量问题的解法（把"必须注入"降级为"慢但可行"）
**分块批处理 + 块间放行**：测出安全窗口（如 1s）→ 每块调用数 = 窗口/单次耗时
→ 每块结束 detach 让游戏跑一会儿 → 循环。
- **AI 分析场景：只需几百个名字 → 几块 → 几秒搞定，加密几乎无影响。**
- 全量 dump（50 万名字）→ 数十分钟到一小时，慢但游戏不死；
  且这是 3% 的罕见兜底路径，可以接受慢（甚至跑一晚上）。

## 交付：功能说明文档已产出

文件：`D:\泪心安卓领域基本盘技术\ue4ImGuiAutoWorkingMcpBytear\泪心UE4_imGui_AnalyseToolsForMCp功能说明.md`

含 10 章：架构总览 / 现状诊断（AutoFix 4 个硬编码卡点 + 依赖链）/ 6 条设计原则 /
约 35 个工具规格（A-G 七组）/ 三个关键场景流程 / 冻结风险评估 / 8 阶段实施路线 /
风险与边界 / 验收标准 / 关键代码位置索引。

**验收标准（已写入文档）**：10 款第三方单机游戏 ≥7 款自主定位成功；
常规 ≤30s、兜底 ≤5min；冻结 ≤ 实测安全窗口 50%；AI 候选判定正确率 ≥90%；
测试期间因工具导致闪退 = 0；单次完整定位 ≤50K tokens。

**下一步待用户确认**：从阶段 0（冻结安全窗口测量，10 分钟）或阶段 1（pattern 库抽取）开始。

## 交付 2：开发必读架构文档

文件：`D:\泪心安卓领域基本盘技术\ue4ImGuiAutoWorkingMcpBytear\泪心UE4_imGui_AnalyseToolsForMCp开发必读架构.md`
（已读完 `awesome-mcp-best-practices` 全部 3 个文件后产出，并在《功能说明》顶部加了指向）

### awesome-mcp-best-practices 内容要点（该仓库多处为 TBD 空标题）
- 1.1 命名 **camelCase 为 preferred**，GPT-4o tokenization 最佳
- 1.2 描述里写明别名（防 LLM 想不起来调用）
- 1.3 描述用 `<use_case>` + `<important_notes>` 模板
- 1.4 **禁止 "not found" 响应**（LLM 会被负面表述带偏而忽略后续有用信息）
- 2.1 提供丰富 Server Instructions（给了 Graphlit 范例）
- 3.1 抽象 Server 能力（IoC，该节内容 TBD）
- 3.2 **不要 1:1 映射 API 到工具**（工具太多 / 轮次太多 / token 太多）
- 另有空标题（无正文）：Testing、Secure Server Code、Avoid descriptive errors、
  Cache costly Tools、Graceful timeouts
- CONTRIBUTING：Conventional Commits、行为变更必须有测试、单 PR 单一逻辑变更、
  自动化 agent 提交 PR 标题末尾加 🤖🤖🤖
- CODE_OF_CONDUCT：Contributor Covenant 1.4，对架构无影响

### 对《功能说明》的修订（**以架构文档为准**）
1. **工具命名全量改 camelCase**（原 snake_case 废弃）—— 附完整映射表（架构文档 §1.1）。
   理由：camelCase 少一个 token，本项目调用频率极高，省下的 token 实在。
2. **工具描述升级为 `<use_case>` + `<important_notes>` 模板**，且必须写"下一步调什么"。
3. **新增 H 组高层用例工具**：`locateEngineGlobals`（一键自主定位，替代 6-8 次调用）、
   `dumpSDK`、`inspectObject`；**新增 I 组复合操作**：`followPointerChain`、`scanCandidates`。
   工具总数 35 → 约 41。
4. **禁止 "not found" 响应**，搜索类工具改为返回次优候选 + 中性描述。
5. **Server Instructions 全文草案**（架构文档 §3）—— 含首选工作流、判定候选的黄金法则
   （真实 FName 表开头必是 None/ByteProperty/IntProperty/... 那批引擎内建名）、硬性约束、易错点。

### 冲突裁决：原语优先 vs 不要 1:1 映射
裁决为**层次化而非二选一**：H 高层（正门）→ I 复合（降轮次）→ 原语（兜底）。
关键论据：3.2 假设 API 已知，而我们是"未知游戏"，必然有高层覆盖不到的情况，原语必须保留；
复合操作直接对冲"轮次太多"的担忧。

### 有理由的偏离（已记录）
- **"避免描述性错误"不采纳**：该建议前提是"多租户/不可信客户端"，本项目是
  本地单用户调试工具，详细错误（含地址、错误码）是核心功能不是风险。
- **Docker 打包不适用**：需 adb + 真机 + root，容器化无收益。
- **capstone 是唯一新增依赖**：需锁版本、可缺失降级，仅 PC 侧使用不参与设备编译。

### 其他定案
- 端口 27185（避开源项目 27184）；错误走 `isError:true` 不用 JSON-RPC error
- 传输与工具分发解耦；工具实现内禁止操作 stdout（日志走 stderr）
- 长任务一律异步返回 jobId；扫描候选按 session 缓存（换判据重评不重扫）
- `executable.cpp` 侵入最小化：仅 main() 起线程 + Layout_tick_UI() poll 队列，
  业务全在 `src/mcp/`

### 结论
**加密对"AI 分析"几乎无影响，只对"全量 dump"有影响，而后者可以慢。**

### 修订后的优先级
1. pattern 库抽取（ptrace 需要函数地址，两者紧耦合）
2. **ptrace 调用接入验证环节** ⬆️（从"最后一档"提到第二）
3. 多锚点接进通用路径
4. MCP 工具规格草案
5. 完整注入 Dobby —— **可无限期延后**（最值钱的能力已由 ptrace 拿到，且不付稳定性代价）

## 架构文档 v1.1 审查修订（MCP 专家视角，12 处问题）

文件已更新为 v1.1（同路径覆盖）。**前三条是事故级**：

### 🔴 灾难级
1. **`beginAttachSession`/`endAttachSession` 是有状态工具，AI 漏调 end → 游戏永久冻结。**
   修复：主推无状态的 `callRemoteFunctionBatch`（内部 attach→N次调用→detach 原子完成）；
   会话模式仅用于交错场景，且四重兜底：maxHoldMs 参数(默认3000/上限30000)、
   空闲看门狗、异常兜底（先 detach 再返回错误）、断连兜底（析构强制 detach）；
   处理目标进程崩溃时 ptrace 返回 ESRCH（清理状态并正常返回，别抛异常）；
   `getCapabilities` 回报 `activeAttachSession` 让 AI 能自我发现恢复。
2. **`writeMemory` 无目标地址校验 → AI 传错地址搞崩游戏。**
   修复：写前用 `KittyPtrValidator`（`KittyMemoryEx/KittyPtrValidator.hpp`）校验
   已映射且 PROT_WRITE，否则拒绝并返回中性说明。
3. **长任务一律异步轮询，快速任务也要 2 次往返，且无取消手段。**
   修复：「短等优先」混合策略（waitMs 默认 5000 上限 60000，超时才返 jobId）；
   新增 `cancelJob`，worker 加协作式取消点。

### 🟠 规范/功能缺失（4-9）
4. `initialize` 补声明 capabilities：tools.listChanged / resources / logging
5. 错误二分：**协议错误走 JSON-RPC error，执行失败走 isError:true**
6. 全部工具补 `outputSchema`，与 structuredContent 严格一致（写单测校验）
7. 补工具 `annotations`（客户端靠 readOnlyHint 自动放行只读工具）
8. 新增 `getLogs`（AI 看不到设备端失败原因就无法自我纠错）+ MCP logging 透传
9. PC 侧与设备端加 `HELLO` 握手 + 协议版本校验（防换版后静默不兼容）

### 🟡 设计/成本（10-12）
10. 补 resources：`umt://processes`、`umt://probe/{pkg}/results`、
    `umt://dump/{pkg}/summary`、`umt://logs`；
    **大文件（SDK_Classes.hpp 几十 MB）禁止做资源**，会撑爆上下文，
    一律走 searchClasses/describeClass 服务端过滤。
11. 工具数 41，约 10K token 常驻上下文，**硬上限 45**；
    新增工具须在 PR 里写明增加的常驻 token。
12. 每个可选参数带 default、数值带 min/max、数组带 maxItems、
    全部 additionalProperties:false。

### 数值表达约定
地址统一用**字符串**（`0x...`）不用 number —— 避免精度问题与进制歧义。

### 工具清单变化（41 个）
- 合并 `getProbeOffsets`+`getProbeStructs` → `getProbeResults`
- 合并 `listElfSymbols` 进 `resolveSymbol(filter?)`
- 新增 `getLogs`、`cancelJob`
- H 组高层正门：`locateEngineGlobals` / `dumpSDK` / `analyzeClass`

## 技术栈定案（架构文档 §5.0，v1.1 新增）

### ❗前提纠正（重要，用户误以为源项目是 FastMCP）
**源项目 `mcp_server.py` 不是 FastMCP，是纯标准库手写**
（import 仅 argparse/json/math/os/socket/struct/subprocess/sys/threading/dataclasses/typing，
无任何 MCP SDK，JSON-RPC 2.0 手工实现）。
→ 它那些限制（无 resources/logging/progress/cancellation、单线程串行阻塞）
**是手写实现的代价，不是 MCP 本身的限制。不要当范本照抄。**

### 选型结论
```
设备端：C++20（已定，无选择余地）
PC 侧 ：Python 3.11+ / 官方 MCP SDK 的 FastMCP（mcp.server.fastmcp）/ capstone
```

**决定项 1：capstone。** `disassemble` 是核心工具（加密 FName 解密靠它读汇编），
Python 有成熟原生绑定；Node 生态弱（capstonejs 长期失修，需 WASM 或原生插件）。
→ **这一条直接排除 TypeScript。**

**决定项 2：SDK 白送 resources / logging / progress / cancellation**，
正是 v1.1 审查要补的四项。手写等于重做 SDK 的活，还容易做错。

- 用**官方 `mcp` 包里的 FastMCP**，不用第三方 `fastmcp` 包（社区版与官方协议节奏不同步）
- **必须锁版本**：MCP 协议一年迭代 4 个版本，SDK churn 快
- FastMCP 同一套工具定义可跑 stdio / HTTP → 直接满足最佳实践 3.1（IoC）

### ⚠️ 两个必须注意的坑
1. **FastMCP 是 asyncio 的，设备 I/O 是阻塞的**（探测可能几分钟）。
   绝不能在 async 函数里直接同步 socket 调用 → 会卡死事件循环，
   progress/cancel/其他工具全部停摆。必须用 `await asyncio.to_thread(...)`。
2. **Pydantic 生成的 schema 必须与文档一致**。41 个工具手写 inputSchema/outputSchema
   极易与实现漂移 → 用类型注解 + Pydantic 自动生成，再写单测反过来校验文档里的 JSON Schema。

### 依赖策略修正
不再声称"零依赖"（引入 capstone 时就不成立了）。改为：
**少而锁定，全部 PC 侧，设备端仍然零新增依赖。**

## 交付 3：pattern 库抽取（阶段 1 已完成）

产出（工作区根目录）：
- `extract_patterns.py` —— 抽取脚本（上游更新时可重跑）
- `ue_pattern_library.json` —— 机器可读，供 `scanPattern` 使用
- `ue_pattern_library.md` —— 人可读报告

运行：`python extract_patterns.py <AndUEDumper/UEGameProfiles> <输出目录>`

### 事实纠正：不是"删除"，是从未移植
查 UMT git 历史，那 24 个文件**从未存在过**（`git log --diff-filter=A` 为空）。
UMT 作者是重写时另选了一批游戏。**不是删 CN 项目。**

### 直接搬运会编译失败（重要）
UMT 基类有 4 个 AndUEDumper profile 没有的**纯虚方法**：
`GetMatrix()` / `GetPhysx()` / `GetFrameCount()`（`= 0`），
AndUEDumper 基类根本没有这三个（UMT 作者后加的）。
→ 移植每个 profile 都要补这三个桩（返回 0 即可，AutoFix 就是这么做的）。

**更关键的架构分歧**：
| | AndUEDumper | UMT |
|---|---|---|
| GetGUObjectArrayPtr / GetNamesPtr | `= 0` 纯虚，每个 profile 写死 pattern | **有默认实现** = 通用自主定位 |

→ 搬一个 AndUEDumper profile 进来 = 用游戏专用 pattern **覆盖掉 UMT 的通用定位**，
与"自主定位"战略相反。**所以正确做法不是搬 profile，是抽 pattern。**

### 精确抽取结果（**修正早期 grep 粗估**）
早期 grep 说的 211/39/42/28 含注释与重复，**不准**。脚本精确去重后：

| UE 版本 | 游戏数 | 原始 | **去重后** |
|---|---|---|---|
| UE4_25_27 | 17 | 130 | **18** |
| UE5_00_02 | 3 | 34 | **14** |
| UE5_03 | 2 | 20 | **14** |
| UE4_23_24 | 3 | 18 | **6** |

17 个游戏只产出 18 条独立 pattern → **反向证实"pattern 是引擎函数序言"**。
（解析 29 个中的 28 个，仅 PUBG 结构特殊未解析）

### 🔴 重大发现：符号定位才是最快的第一路径（v1.0 漏了）
**28 个 profile 里 19 个的第一策略是符号定位，不是 pattern。**
ArkUltimate 的写法是：先 `findSymbol("GNameBlocksDebug")`，失败才走 idaPatterns。

四个引擎原生符号（跨版本稳定）：
| 符号 | 用途 | 覆盖 |
|---|---|---|
| `GUObjectArray` | 对象数组 | **全版本** |
| `GNameBlocksDebug` | 名字表（解引用后回减 FNamePool.BlocksOff） | UE4.23 → UE5.03 |
| `_ZN5FName16GetIsInitializedEv` | 名字表（需 DecodeADRL） | UE4.00–4.20 |
| `GFNameTableForDebuggerVisualizers_MT` | 名字表（直接即地址） | UE4.22 |

**修正后的定位优先级（已写入两份文档）**：
```
① 符号定位    O(1) 最快最稳  ← v1.0 漏掉的就是这条
② pattern     秒级，按版本挑
③ 结构扫描    分钟级，兜底
④ ptrace 调用 加密时让游戏自己解
```
理由：符号表查找 O(1) vs pattern 扫段 O(n)；符号名是源码标识符，比字节序列稳定。

### 未解析
`PUBG.hpp` —— 结构特殊（自定义逻辑），未适配解析器。

## 澄清：「不搬 profile」≠「不写新代码」（用户追问，已澄清）

用户问 `Andriod_UnrealMemoryTools/src` 是否无需补充新代码/新逻辑。
**答案：需要，而且很多（设备侧约 3000 行 C++ + PC 侧约 900 行 Python）。**
（注：我上一条只说了不搬 profile，表述不清导致误解，已向用户说明。）

### 硬核实证据（已 grep 验证）
| 能力 | 位置 | 状态 |
|---|---|---|
| `ElfScanner::findSymbol` | `KittyScanner.hpp:211` | **存在但 `src/` 零调用** |
| `KittyTraceMgr`（ptrace 远程调用） | `KittyTrace.cpp:140` | **存在但 `src/` 零引用** |
| socket 服务 | — | **完全不存在** |

`src/` 里 `GUObjectArray` 仅 2 处（`executable.cpp:437`/`:491`），
都是**输出标签**不是符号查找 → **UMT 从未走过符号定位这条最快路径。**

### 已写进架构文档 §5.6（代码清单 + 估算）
设备侧约 3000 行，最大头：命令服务 400 / 参数化扫描 400 / 类索引 300 /
ptrace 胶水 300 / 高层用例 350 / 采样 200 / 队列分发 250 / 符号定位 150 /
复合操作 150 / Job 管理 150 / 多锚点改造 80 / executable.cpp 接线 15。

## 交付 4：任务 1 文档（用户要求"罗列任务1"）

文件：`D:\泪心安卓领域基本盘技术\ue4ImGuiAutoWorkingMcpBytear\泪心UE4_任务1_GetNamesPtr多锚点改造.md`

### 关键事实（写代码前已核实）
- `GetNamesPtr()` 位于 `src/UE/UEGameProfile.cpp:971-1084`
- `isByteProperty` lambda `:999-1002`
- `tryFNamePool` lambda `:1028-1040`（只读 `val + 0x8`）
- `tryGNames` lambda `:1042-1066`（只读 `p1 + 0x24`；PUBG 变体 `:1052-1063` 读 `q3 + 0x24`）
- 偏好顺序分支 `:1068-1077`
- **可复用的 12 个引擎内建名名单在 `:80-84`，是 `BruteForceDeltaForceNames()`
  （起始 `:64`）的函数内局部 static，`GetNamesPtr()` 看不到** → 需提到文件作用域

### 任务 1 方案要点
- 改动 A：名单提文件作用域 + 新增偏移候选表
  `kPoolEntryOffsets = {0x8,0x6,0xA,0x4,0xC,0x10}`
  `kGNamesEntryOffsets = {0x24,0x2C,0x1C,0x14,0x34}`
  `kMinAnchorHits = 2`
- 改动 B：`MatchEngineName()` 匹配 12 个引擎名任一
- 改动 C：**两级判定** —— Tier1 精确匹配（遍历偏移表，命中即停）；
  Tier2 窗口扫描兜底（在 96 字节窗口内统计不同引擎名数量 ≥ 阈值）
- 改动 D：改接 tryFNamePool / tryGNames，日志补实际命中的 anchorOff

### 明确不做（防范围蔓延）
指针范围放宽(:995-996)、符号定位接线 → 任务 2；
扫描区域改全内存、解耦 GUObjectArray 依赖 → 任务 5。

### ⚠️ 前置条件（已写进文档）
`CMakeLists.txt:2` 硬编码 `set(NDK_PATH E:/ndk/android-ndk-r29)`，
但用户 NDK 在 `D:/ProgramerDevelop/ndk23` 且项目要求 NDK r25+。
**先确认能构建再改代码**，建议改成 `if(NOT DEFINED NDK_PATH)` 可覆盖形式。

### 验收标准
1. 编译通过
2. **自有游戏定位成功且地址与 ground truth 一致**（用户独有优势）
3. 至少 2 款原本成功的游戏回归不破
4. 指向非 UE 进程不得返回非零（无新增误报）
5. 日志含实际命中的 anchorOff

### 已创建的任务清单（TaskCreate）
- #1 任务1：GetNamesPtr 多锚点改造（~85 行）
- #2 任务2：指针范围放宽 + 符号定位接线（~150 行）
- #3 任务4：设备端命令服务与队列（~650 行）
- #4 任务5：PC 侧 MCP Server（FastMCP）（~900 行）
（任务 3「pattern 库接入」数据已备好，任务 6/7 见架构文档 §5.6.4）

### 建议实施顺序（已写入文档 §5.6.4，每项可独立验证）
```
① 多锚点改造 80 行      ← 最小改动，最先验证价值
② 符号定位接线 150 行    ← 最快路径，性价比最高
③ pattern 库接入         ← 数据已备好
④ 命令服务+队列 650 行   ← 打通链路，此后才能远程驱动
⑤ 参数化扫描+采样 600 行 ← AI 闭环核心
⑥ ptrace 胶水 300 行
⑦ 类索引+高层用例 650 行
```

## 任务 1 代码完成核查、编译跑通与提交推送（本会话补充，2026-08-29）

### 代码核查结论：任务 1 彻底完成，实现优于方案
逐项对照《任务1文档》方案，全部落地（行号为改造后实际位置）：

| 改动项 | 状态 | 实际位置 |
|---|---|---|
| A 名单提文件作用域 | ✅ | `UEGameProfile.cpp:68`（沿用名 `kKnownNames`），`BruteForceDeltaForceNames` `:263` 已共用 |
| B `MatchEngineName` | ✅ | `:92` |
| C 两级判定 | ✅ | 合并为 `ScanEngineNameAnchors()` `:115`，返回 `AnchorScanResult{hit,exact,offset,hits}` |
| D `tryFNamePool`/`tryGNames`+PUBG 变体 | ✅ | `:1122-1147` / `:1149-1209`（PUBG `:1177-1206`） |
| E 删 `isByteProperty`、日志带偏移 | ✅ | 日志含 `anchorOff` + `hits` |

**两处优于方案的实现**：
1. **单次远程读**：一次读 `kAnchorWindowSize=128` 字节后本地跑两级判定，
   比方案"每个偏移读一次"（最多 6 次远程读）省。
2. **宽松命中不盖精确命中**：Tier2 命中先记 `weakPool`/`weakGNames`（`:1095-1096`），
   整轮扫完无精确命中才回退使用（`:1226-1235`，`LOGW` 提示用 probe 复核）。

备份文件已改名 `src/UE/UEGameProfile未改动代码.cpp.bak`，
不会被 `file(GLOB src/UE/*.cpp)` 误编译（未提交，留本地）。

### 编译验证：NDK 踩坑 → 解决（已跑通 ✅）
本机构建工具：PATH 无 cmake/ninja，用 **VS2026 自带**：
`D:\ProgramerDevelop\VS2026\SDK\Common7\IDE\CommonExtensions\Microsoft\CMake\`
（CMake\bin\cmake.exe + Ninja\ninja.exe）。

- **第一次**：`D:\ProgramerDevelop\ndk23` 实为 **r23.2**（source.properties: 23.2.8568313），
  低于项目要求 r25+。`UEGameProfile.cpp`（任务1 唯一改动文件）单独编译通过，
  但整项目挂在**未被本次改动的** `Dumper.cpp`：
  ① `json.hpp:6094` `u8string()` 类型不匹配（r23 libc++ 的 `filesystem::u8string()` 返回
  `std::string`，`nlohmann/json` C++20 写法期望 `std::u8string`）；
  ② `Dumper.cpp:427` `operator==` 二义性。均为工具链版本问题。
- **第二次（用户提供新 NDK 后）**：`D:\ProgramerDevelop\windowsNDK27` = **r27d**
  （Pkg.Revision=27.3.13750724），重新配置后 **51/51 编译链接全部通过**，
  两个报错一行代码没改即消失 → 反向确认与任务 1 改动无关。
  产物：`outputs/arm64-v8a/UnrealMemoryTools`（约 3.2 MB）。

构建命令（已写入任务1文档 §5）：
```
cmake -S . -B build -G Ninja -DNDK_PATH=D:/ProgramerDevelop/windowsNDK27
cmake --build build
```

### CMakeLists.txt 修改（已生效并入库）
第 2 行硬编码改为可覆盖形式：
```cmake
# 允许构建时覆盖：-DNDK_PATH=D:/ProgramerDevelop/ndk23
if(NOT DEFINED NDK_PATH)
    set(NDK_PATH E:/ndk/android-ndk-r29)
endif()
```

### Git 提交与推送（已完成）
仓库根：`D:\泪心安卓领域基本盘技术\ue4ImGuiAutoWorkingMcpBytear`
（注意不是 Alltear_UnrealMemoryToolsNewMcp 子目录），远程
`git@github.com:tearhacker/UE4Dump_imGui_AnalyseToolsForMCP.git`，分支 main。

- 任务1 的代码改动本身随 `9d4f848 初始化完整源码` 已在仓库中。
- 本会话提交 `066976b` 并已推送：**build(ue): 任务1收尾**，含 5 个文件：
  `.gitignore`（新建，忽略 `Alltear_UnrealMemoryToolsNewMcp/build/`）、
  `CMakeLists.txt`、备份 `.cpp` 删除、任务1文档更新、
  `outputs/arm64-v8a/UnrealMemoryTools` 产物入库（用户要求产物进仓库）。
- `.bak` 备份文件按惯例未提交。

### 任务1文档同步更新（已交付）
顶部状态标记、§3 实现与方案差异对照表、§5 前置条件实测记录
（r23 踩坑 + r27d 解决 + 构建命令）、§6 验收状态标注、文末行号速查表
全部换为改造后实际位置。

### 当前待办
1. **真机验收（任务1 验收标准 2-4，只有用户能做）**：
   自有游戏 ground truth 对比（看 `anchorOff` 实际落点）/ ≥2 款回归 / 非 UE 进程返回 0。
2. 验收数据回填《功能说明》§9。
3. 开任务 2：指针范围放宽 + 符号定位接线（~150 行，定位优先级里最快的路径）。

## 会话续接：项目现状盘点（2026-08-29 07:10）

### 仓库事实（已核实，纠正两处认知）
- **远程是 GitHub 不是 Gitee**：`git@github.com:tearhacker/UE4Dump_imGui_AnalyseToolsForMCP.git`，
  只有一个 origin，无 Gitee 远程。
- **仓库根 = 工作区根** `ue4ImGuiAutoWorkingMcpBytear`，
  `Alltear_UnrealMemoryToolsNewMcp/` **没有自己的 .git**（是根仓库的子目录）。
- 分支：本地 main / master 均存在，HEAD=main；`main` 与 `origin/main` **完全同步**（0/0）。
- 未推送的只有未跟踪文件
  `Alltear_UnrealMemoryToolsNewMcp/src/UE/UEGameProfile未改动代码.cpp.bak`（惯例：备份不入库）。

### 提交历史（共 5 个）
`63204ad 初始化项目` → `a9cde9c 文档` → `9d4f848 初始化完整源码`（381 文件，含任务1代码）
→ `066976b build(ue): 任务1收尾` → `241ca80 docs(memory)`（最新，08-29 06:41）。

### 代码侧现状（目录级确认）
- `src/` 下**没有 mcp/ 目录** → 设备端命令服务（任务4）**尚未开工**。
- `outputs/arm64-v8a/UnrealMemoryTools` 3.2 MB，08-29 06:21 产物，已入库。
- 工作区根目录已有 `extract_patterns.py` / `ue_pattern_library.json` / `ue_pattern_library.md`（任务3 数据就绪）。
- 七步实施顺序里**只有 ① 完成**，②-⑦ 全部未开始。

## UMT 的 UEGameProfile.hpp 对照上游 AndUEDumper 的缺口（2026-08-29 07:15）

对照 `源参考AndUEDumper/AndUEDumper/src/UE/UEGameProfile.{hpp,cpp}` 逐方法比对。

### UMT 缺失的 4 个上游方法
| 方法 | 上游位置 | UMT 现状 | 影响 |
|---|---|---|---|
| `GetUESoNames()` | hpp:39 / cpp:212 | ❌ 无，`GetUnrealELF()` 里硬编码 `{libUE4.so, libUnreal.so}`（cpp:869-870） | lib 改名/静态链接 → 直接 ERROR_LIB_NOT_FOUND（记忆里的 case ③） |
| `findProcessEvent()` | hpp:58 / cpp:261-397 | ❌ 无，UMT 用自研 `GetProcessEvent()`（cpp:1305-1336） | 见下，定位脆弱点 |
| `GetExcludedObjects()` | hpp:61 / cpp:464 | ❌ 无（上游 Dumper.cpp:344 调用） | dump 无法排除对象 |
| `GetUserTypesHeader()` | hpp:64 / cpp:474 | ❌ 无 | 上游自己也没调用（仅定义），优先级最低；UMT 的 TArray 在 `UEWrappers.hpp:25` 有内部版 |

### `GetProcessEvent()`（UMT cpp:1305-1336）的三个硬伤
1. 只扫 vtable 前 500 项，且**写死 `i > 50 && i < 100`** —— ProcessEvent 不在该下标区间就返回 0
2. 判定条件 = 连续三条指令均为 STP/STR（`IsStpFpLr` / `IsStrInstruction`），编译选项一变就失配
3. 依赖 `UEWrappers::GetObjects()` 已初始化，取 index 0 对象的 vtable

上游 `findProcessEvent()` 做法（更鲁棒）：先查符号
`_ZN7UObject12ProcessEventEP9UFunctionPv` 命中即返回；否则每个虚函数读 0x200 字节，
按 10 项指标打分（ADRP 是否指向 GUObjectArray/ObjObjects、UObject.InternalIndex、
FUObjectItem.Size、EFunctionFlags+1/+2、UStruct.PropertiesSize、UFunction.ParamSize×2、
ChildProperties×2），取最高分，并回报 ProcessEventIndex。

### UMT 优于上游（不要改回去）
- `GetUnrealELF()` 多了 split config：apk 内 zip 查找（cpp:872-897），上游没有
- `GetNameEntry()` 修了上游 bug：上游 `static uintptr_t gNames` 只判 `== 0`，换目标不失效；
  UMT 用 `gNamesPtr != namesPtr` 失效判断（cpp:752-768）+ PUBG 多级解引用
- `GetGUObjectArrayPtr()` / `GetNamesPtr()` 有通用默认实现（上游是纯虚 = 0）

### 疑似 bug：UMT `isEmulator()`（cpp:911-922）
若 `GetUnrealELF().segments()` 为空（ELF 头被抹/段信息拿不到），for 循环不执行，
直接 `return true` → 把所有这类游戏误判为模拟器，pattern 搜索从 ANY_X 退化成 ANY_R。
上游用 `elfScanner.isElfEmulated()` + `nbScanner.isValid()`，不依赖 segments。

### 定案
以上**不属于任务 1**（任务1 只覆盖 GetNamesPtr 多锚点），建议立为任务 8。
性价比排序：① `GetUESoNames()` 提出来（5 行）② `findProcessEvent()` 移植（~140 行）
③ `isEmulator()` 修 bug（~10 行）④ `GetExcludedObjects` ⑤ `GetUserTypesHeader`（可不做）。

## 🔴 第一次真机验证结果（2026-08-29 07:2x）—— 任务1 部分成功，战略方向被证实

测试对象：第三方游戏 `com.tencent.ngr`（UE 4.26.2）。
**历史上第一次在真机第三方游戏上跑通任务1 产物。**

### 日志原文
```
[AutoFix] UE version: ++UE4+Release-4.26
[Bootstrap] FNamePool @ 0x79fca74868 (slot @ 0x79fca748a8, anchorOff=0x10, hits=1)   ← 出现两次
E: [Bootstrap] 通用方式搜索 GUObject 失败
E: 探针失败: ERROR_INIT_GUOBJECTARRAY
```

### 验收结论
| 标准 | 结果 |
|---|---|
| ① 编译通过 | ✅ 51/51（07:23 重编，产物 3,249,008 字节，与入库版一致） |
| ② ground truth 对比 | ⚠️ **部分成功**：Names ✅ / GUObject ❌ |
| ⑤ 日志含实际命中 anchorOff | ✅ **0x10** |

### 两个决定性事实
1. **任务1 多锚点改造在真机上生效**：`anchorOff=0x10` —— 旧代码硬编码的 `0x8` **是错的**，
   多锚点候选表里的 0x10 命中；`hits=1` = Tier1 精确匹配（非 Tier2 宽松兜底）。
   **若沿用旧单锚点+0x8，该游戏直接返回 0、探测全崩。**
2. **GUObjectArray 失败是独立的老问题，不是任务1 引入的回归**。
   此前被 GNames 失败掩盖（GNames 一挂，GUObject 根本轮不到执行）。
   `GetGUObjectArrayPtr()`（cpp:983-1059）从 FNamePool 往低地址只扫 **0x300000（3MB）**，
   且验证条件是"第一个对象名字**精确等于** `/Script/CoreUObject`"，零容错。

### 🔥 战略定案（用户拍板，后续必须遵守）
用户明确表态：**不要靠静态写代码猜偏移去修 GUObjectArray，
要靠未来扩展的 MCP 工具 + AI 大模型分析解决。** 与架构文档既有设计完全一致。

**这次日志是该战略最硬的两条证据**：
- `anchorOff=0x10 ≠ 硬编码 0x8` → 证明"静态猜偏移"必然在某些游戏上失效
- GUObject 3MB 静态扫描 + 零容错字符串比对失败 → 证明"固定启发式"容错为零

**关键差异（写文档时引用）**：AI 判"这一串名字像不像 UE 对象表"，**容忍其中几个读错**；
静态代码要求"精确等于 /Script/CoreUObject"，**一个字符不对就 return 0**。
→ 这正是"AI 是能容忍噪声的验证器"的实证。

### 唯一硬前置：命令服务（任务 ④）
AI 再聪明，**没有远程通道就够不着设备**。要让 AI 能换假设/看样本/判结果，
必须先有 `src/mcp/CommandServer`（socket + HELLO 握手 + 队列，约 650 行），
否则 `scanGNames`/`sampleGNames`/`scanObjects`/`sampleObjects` 全部无法被远程驱动。

### 若有 MCP 工具，AI 破此案例的路径（供写文档引用）
1. `resolveSymbol` 查 `GUObjectArray` → 命中即 O(1)（UMT 至今从未走过这条路）
2. 无符号 → `scanPattern`（ue_pattern_library.json 里 UE4_25_27 有 18 条）→ DecodeADRL → 候选
3. 仍无 → `scanCandidates`（**参数化**：指针范围/扫描区域=全内存，而非写死 3MB）→ 候选集
4. `sampleObjects` + `sampleGNames` → 取每个候选前 N 个名字 → **AI 看语义**判定
5. 收敛或换参数重扫（AI 换假设，**不需要改代码重编译**）

## 两份设计文档的过时点清单（2026-08-29 通读后发现，待更新）

架构文档 785 行 / 36KB，功能说明 464 行 / 20KB。核心设计（四层结构、定位优先级
符号>pattern>结构扫描>ptrace、验证纪律、上下文预算、无状态 attach、41 工具清单）
**全部仍然成立，不需要改**。以下只是"事实层"需要回填：

### 🔴 会误导开发的硬错误
1. **《功能说明》§10 行号索引全部失效** —— 记的是改造前的位置：
   `GetNamesPtr():971`→实际 **1061**；指针范围 `:995-996`→实际 **1085-1086**；
   `kKnownNames :80-84`→实际 **68**；`GetGUObjectArrayPtr():893`→实际 **983**；
   `findIdaPattern:834`→实际 **924**。（任务1 后该文件 1181→1336 行）
2. **《功能说明》§5.1 场景流程写"用 211 条 UE4.25-27 pattern"** —— 错，去重后是 **18 条**。
   §2.4 末尾虽有警告，但 §5.1 正文没改。
3. **《功能说明》§2.2"四个硬编码卡点"** —— 锚点偏移与单锚点两项已被任务1 修复，
   文档未标注（指针范围 0x4FFFFFFFFF~0x7FFFFFFFFF 仍是任务2 的范围）。

### 🟠 状态需要回填
4. §2.3"白捡的改进点…可能立刻救活一批游戏"—— **已实施且已真机验证**
   （`anchorOff=0x10` 打脸硬编码 `0x8`）。这句推测现在有实证，应改为结论。
5. §7 实施路线：阶段1（pattern 抽取）✅、阶段2（多锚点）✅已验证、
   阶段0（冻结安全窗口）❌仍没做、阶段3 起未开始。
6. 《架构》§5.6.4 实施顺序 ① 应标注"已完成 + 真机部分成功"。
7. **§8.3 是个空的"NULL待补充"占位符**。

### 🟡 可补充的实证
8. §1 现状诊断可补具体案例：`com.tencent.ngr`（UE 4.26.2）Names 成功、
   GUObjectArray 失败（3MB 静态扫描 + 零容错字符串比对）。
9. §9 验收标准可补第一条实测记录。

### 已知且已有顶部警告（不必改）
《功能说明》§4 正文仍是 snake_case（`memory_read`/`names_scan`），
顶部已声明"以架构文档 §1.1 的 camelCase 为准"。

## 🔴 设备端能力全量审计（2026-08-29 07:40，决定 MCP 前置条件）

10 项逐条核查（Explore agent 深度扫描 + 人工复核）。**结论：可加 MCP，无架构级阻塞。**

| # | 能力 | 结论 | 证据 |
|---|---|---|---|
| 1 | socket/命令服务 | **完全不存在**（0 命中 AF_INET/bind/listen/accept） | `main()` 在 `executable.cpp:1805-1813` 直接进 Vulkan 渲染阻塞循环 |
| 2 | `ElfScanner::findSymbol` | 存在但 **src/ 零调用** | 声明 `KittyScanner.hpp:211` |
| 3 | ptrace 远程调用 | 存在但 **src/ 零引用**（死代码） | `KittyTrace.cpp:140` |
| 4 | 参数化扫描 | 存在但**参数全硬编码**、无参签名 | `UEGameProfile.hpp:63` / `:55` |
| 5 | 批量采样 | 存在但**硬编码只取 5 条** | `Dumper.cpp:248-251` / `267-271` 均 `for(i<5)` |
| 6 | 类/对象检索 | **强耦合 ImGui 且不可导出** | `SDKExplorer.cpp:18-19` 内层匿名 namespace；250 处 `ImGui::` |
| 7 | Probe/Dump 触发 | 底层可独立调用，**编排层不可** | `ExecuteProbe`(644)/`ExecuteDump`(754)/`ExecuteDumpUnrealLib`(859) 在 `executable.cpp:51-990` 匿名 namespace |
| 8 | 日志取回 | 有 1500 行环形缓冲，**外部取不到** | `PushUiLog`(227-237) 写 `gDumpUiState.logLines`，`kMaxLogLines=1500`(54) |
| 9 | 任务取消 | **完全不存在** | `cancelRequested`/`atomic<bool>` 在 src/ 非 imgui 部分 0 命中 |
| 10 | 进程枚举 | **存在且可用** | `FindAutoProcessCandidates()` @ `executable.cpp:166-216`，无参，返回 `{pid, package, profileName, dedicated}` |

### 🔥 两个重大新发现（此前未知）

**① 匿名 namespace 封锁 —— 真正的成本不在"重写"而在"外迁"**
`executable.cpp:51-990` 与 `SDKExplorer.cpp:18-19` 两个匿名 namespace，
把 7/8/10 三项**已经写好的现成实现**全锁死在 TU 内部。
→ 外迁约 **200-300 行**即可（不是重写）。这大幅低于架构文档 §5.6.3 的估算。

**② `GetGUObjectArrayPtr()` 只往高地址单向扫描 —— 疑似真 bug**
```cpp
// UEGameProfile.cpp:1010-1012
for (int i = 0; i < 0x300000; ++i)
    const uintptr_t candObjAddr = namesScanBase + 8ULL * i;   // 只有 +，没有 -
```
若 GUObjectArray 落在 FNamePool **低地址**方向，则**永远扫不到**。
这很可能就是 `com.tencent.ngr` 失败的直接原因（不是范围不够大）。

**⚠️ 纠正我先前的错误**：我此前说"只扫 3MB"是错的。
`0x300000` 次 × 8 字节 = **0x1800000 = 24MB**，且方向单向。
真正的病根更可能是**单向 + 零容错验证**，不是范围小。

### 其他硬编码实况（参数化的具体靶点）
`GetNamesPtr()`：指针范围 `0x4FFFFFFFFF~0x7FFFFFFFFF`(1085-1086)、分块 `0x100000`(1087)、
**扫描区域仅 `ue_elf.segments()` 的 readable 段**(1098-1101，不能扫全内存)、步长 8(1110)。
`GetGUObjectArrayPtr()`：循环上限 `0x300000`(1010)、指针下限 `0x10000`(1015/1031) 无上限、
名字偏移候选 `kNameOffs[] = 0x18~0x6c` 步长 4 共 22 个(1008)、
FName id 范围 `1~0x200000`(1037)、锚点字符串硬编码 `"/Script/CoreUObject"`(1041)。

### 纠偏
- ptrace 成员名不是 `kTraceMgr`，是 `KittyMemoryEx/KittyMemoryMgr.hpp:47` 的 `KittyTraceMgr trace;`
- `UEDumper::Init(IGameProfile*, bool reuseInitializedState=false)` / `Dump(...)`（`Dumper.hpp:31/33`）
  **是 public 且无 UI 依赖**的干净底座，可直接调用

### 工作量重估
架构文档 §5.6.3 估"约 3000 行"，精确核查后**设备端约 1200-2000 行**：
- 地基（socket 命令服务 + 队列 + 分发）**从零写**：500-850 行
- 打通（外迁 4 处匿名 namespace）：200-300 行
- 能力（findSymbol 接线 80-150 / 参数化 60-120 / 批量采样 40-80 / 取消 80-150 / 日志 40-80）：300-500 行
- 检索层与高层用例：200-400 行
- **另需 `CMakeLists.txt:22-33` 的 GLOB 新增一行**挂 `src/mcp/*.cpp`

### 风险
启用 ptrace 会与现有"无注入"设计冲突，也更容易被反作弊识别（单机游戏场景可接受）。

### 审计结论：MCP 的四层前置（下层是上层的硬前置）
```
L1 体验层  SDK 类检索 / 高层用例 / ptrace 调用        可延后
L2 能力层  findSymbol 接线 / 参数化扫描 / 批量采样      ~500 行，决定 AI 上限
L3 打通层  外迁匿名 namespace（进程枚举/日志/Probe编排） ~300 行，现成的仅差导出
L4 地基    socket 命令服务 + 队列 + 分发                ~700 行，从零写
```
**没有 L4，上面三层全都够不着设备。**

## 交付：新建《泪心UE4_MCP工具功能规格.md》（2026-08-29 08:10）

用户嫌《功能说明》太杂（混了现状诊断/设计论证/实施路线/验收标准/行号索引），
要求写一份**只讲 MCP 具体功能、按模块划分**的文档。

路径：`D:\泪心安卓领域基本盘技术\ue4ImGuiAutoWorkingMcpBytear\泪心UE4_MCP工具功能规格.md`

### 文档定位（三份文档的分工，后续沿用）
| 文档 | 回答什么 |
|---|---|
| **《MCP工具功能规格》**（新） | **MCP 对外提供哪些工具、每个做什么** —— 纯功能，不含论证 |
| 《功能说明》 | 做什么、为什么做（现状诊断/设计原则/实施路线/验收标准） |
| 《开发必读架构》 | 怎么写（命名/描述模板/协议/目录结构/检查清单） |

### 文档结构
A 连接与能力(2) · B 进程(2) · C 流程与产出(11) · D 内存原语(7) · E 理解层(2) ·
F 远程调用(5) · G 引擎语义(8) · H 高层用例(3) · I 复合操作(2) = **42 个**

每个工具统一格式：用途 / 输入 / 输出 / **实现基础** / 约束 / 下一步。
附录：编码约定 · 错误分层 · 禁止 not found · 判定权在 AI · 短等优先 · 缓存 · 成本汇总。

### 本文档特有：实现基础四档标记（判断成本的核心）
| 标记 | 含义 |
|---|---|
| ✅ 已有 | 设备端已具备，只需封装 |
| 🔧 外迁 | 已实现但锁在匿名 namespace，需导出 |
| ⚠️ 新建 | 设备端没有，从零写 |
| 🖥️ PC 侧 | 不需设备端参与 |

### ⚠️ 发现架构文档的数字错误
《开发必读架构》§1.1 标题写"41 个工具"，但表格里
A2+B2+C11+D7+E2+F5+G8+H3+I2 实际是 **42 个**。新文档按 42 写，未改架构文档（待用户确认）。

### 记录到文档里的关键事实（都来自本次源码审计）
- `resolveSymbol` 标记 🔧 需接线，写明四个引擎原生符号与"strip 后不得返回 not found"
- `scanObjects` 标记 ⚠️ 并写明 **`direction` 参数默认 BOTH，修单向扫描 bug**
- `searchClasses`/`describeClass` 明确"不可复用 SDKExplorer.cpp，需另起无头 SDKQuery 层"
- `cancelJob` 标记 ⚠️ 新建，写明需在两个扫描热循环插 `atomic<bool>` 检查点
- `getLogs` 标记 🔧 外迁，写明设备端已有 1500 行环形缓冲
- F 组整组标注风险：与 `/proc/pid/mem` 无注入设计冲突、易被反作弊识别
- 成本汇总表：设备端 1200–2000 行，硬前置是 socket 命令服务 500–850 行

## 🔴 全项目功能盘点 & MCP 覆盖度缺口（2026-08-29 08:20，用户直觉触发）

用户质疑"MCP 功能少了"，全量盘点后**确认属实，且缺口很大**。
42 个工具只覆盖了"定位 + 读内存"，UMT 真正值钱的两大块**几乎没进 MCP**。

### MCP 对六大模块的覆盖度
| 模块 | 规模 | MCP 覆盖 |
|---|---|---|
| 内存原语（KittyMemoryEx） | — | ✅ ~90%（D 组 7 个） |
| 进程/探测/转储 | executable.cpp UI | ✅ ~75%（C 组 11 个） |
| 产物读写 | 10 类文件 | ⚠️ ~60%（仅通用 list/read） |
| UE 对象导航（UEWrappers 1651 行） | — | ⚠️ ~30%（仅 search/describe/inspect 3 个） |
| **SDK Explorer（1455 行）** | — | ❌ **~10%** |
| **AutoFix 结构推断（3185 行）** | — | ❌ **~5%** |

### 缺口 1：AutoFix 子系统几乎完全没进 MCP（最大遗漏）
唯一入口 `AutoFix::RunFixup(IGameProfile*)`（`AutoFixOffsets.hpp:13` / cpp:3116，
由 `Dumper.cpp:135` 调用），编排在 cpp:3131-3167 且**在匿名 namespace，不可单独调用**。

能自动推断（这些能力 MCP 一个都没暴露）：
- UObject 全套：NamePrivate / ObjectFlags / InternalIndex / ClassPrivate / OuterPrivate
- FName 全套：Size / ComparisonIndex / Number / CasePreserving / OutlineNumber（`FixupFNameSettings` cpp:993）
- UStruct：SuperStruct / PropertiesSize / MinAlignment / Children / ChildProperties
- UField.Next；UClass：CDO / ImplementedInterfaces / CastFlags / ClassFlags
- ULevel.Actors；UDataTable.RowMap
- FField / FFieldClass / FProperty 布局（`FixupFPropertyLayout` cpp:2989）
- UEnum.Names；UFunction 布局与 Func

**可独立调用的子能力（都是现成的，MCP 完全没用上）**：
| 子能力 | 位置 | 能做什么 |
|---|---|---|
| `AutoFixStructLayout` | StructLayout.hpp | Size/Alignment 推断、父类对齐修正、**位域 bool bit index**、GetSortedFields |
| `AutoFixPropertyOffsets` | PropertyOffsetFinder.cpp:318-463 | 13 项子属性偏移：ObjectProperty.PropertyClass / StructProperty.Struct / ByteProperty.Enum / BoolProperty.Base / EnumProperty / DelegateProperty.SignatureFunction / ArrayProperty.Inner / SetProperty.ElementProp / MapProperty.KeyProp+ValueProp / ClassProperty.MetaClass / InterfaceProperty.InterfaceClass |
| `AutoFixVTable` | VirtualFunctionResolver.hpp | 虚表槽位/索引/直接跳转目标/读虚表函数 |
| `AutoFixNativeFunctions` | NativeFunctionResolver.hpp | FuncOffset / VTableOffset / RealOffset，CDO 扫描 + 接口类 vtable 解析 |
| `AutoFixArm64` | Arm64Decode.hpp | ADRP/ADD/LDR/BL/B/BLR/BR/RET/MOVZ 解码 |
| `DumpAutoFoundOffsets` | AutoFixOffsets.cpp:504 | **AutoFix 前后偏移对比**（诊断利器） |

### 缺口 2：SDK Explorer 的交互能力基本没进 MCP
- **容器视图**（cpp:1246）：Array/Set/Map 的 Num/Max/Data **逐元素读值**，MaxRows 1-4096 ← AI 分析最需要
- **运行时视图**（cpp:1007）：UWorld 的 PersistentLevel / OwningGameInstance / NetDriver
  + Actor 预览（cpp:875）+ ULevel Actor 列表 + UDataTable RowStruct/RowMap
- 对象浏览器（316-455）：分页 10-1000、异步搜索带命中计数与进度
- 函数列表（523）：Signature / Flags / Num / Size / Func / RVA / Owner
- 成员视图（1106-1243）：点 ObjectProperty 值可跳转下级对象；`ReadFieldValue`(598)

### 缺口 3：UEWrappers 对象导航只用了零头
现成可用但 MCP 未暴露：`FindObject`(全名) / `FindObjectFast`(短名) /
`FindObjectFastInOuter` / `ForEachObject` / `ForEachObjectOfClass` /
`NumElements` / `IsA` / `GetFullName` / `GetCppName` / `HasFlags`
（`UEWrappers.hpp:227-542`）。MCP 只有 searchClasses/describeClass/inspectObject 三个。

### 其他事实
- 8 个内置 profile：暗区突围/三角洲/远光84/枪战特训2/无畏契约/洛克王国:世界/和平精英HD/和平精英
- 产物 10 类：Logs.txt / Offsets.hpp / Objects.txt / AIOHeader.hpp / SDK_Enums.hpp /
  SDK_Structs.hpp / SDK_Classes.hpp / SDK_Offset.hpp / script.json / libUE4.so
  （`script.json` 含 `Functions:[{Address,Name}]`，可直接喂 IDA/Ghidra 脚本）
- `InvalidateProbeReuse()`（executable.cpp:282）进程切换自动作废旧探针
- **无**偏移编辑 UI / **无** profile 管理 UI / **无**截图 / **无**配置持久化
  （`AndroidImgui.cpp:20` `io.IniFilename=nullptr`）
- `src/Utils/KittyCmdln.cpp` 命令行框架**是死代码**（`main()` 无参不解析 argv）

### ⚠️ 由此暴露的架构决策点
架构文档 §1.7 定**硬上限 45 个工具**。现有 42 个，补齐上述缺口需再增 8-10 个 → 50-52 个，**撞线**。
三个解法待用户拍板：① 突破上限（论证：10K token 常驻可接受）
② 合并现有工具腾位 ③ **分组按需挂载**（核心常驻 + 其余靠 `tools.listChanged` 动态加载）← 我倾向③

## 🔑 用户认知定案：核心任务 = 写 API 接口文档（2026-08-29 08:25，重要）

用户自己总结出的心智模型，**后续按此执行**：

> "分析源码 → 了解每个功能 → 针对每个功能写接口（函数签名 + 作用介绍）
>  → 本质上就是写 API 接口文档，跟开发全栈 web 一个道理"

### 全栈类比（后续沟通沿用这套词汇）
```
全栈 web                    UMT MCP 项目
数据库 + 业务逻辑      →    设备端 C++ 源码（UMT 现有能力，已齐）
Service 层函数         →    src/mcp/Commands/*.cpp 的 handler      ← 缺
HTTP 路由 / 协议       →    命令分发器（socket + JSON）             ← 缺
REST API 文档(Swagger) →    《设备端API接口清单》                   ← 本次产出
前端调用               →    MCP 工具 → AI 调用
```
**关键纠正**：我此前纠结"AI 需要哪些工具"是顺序错了。
正确顺序 = 先盘清"源码有什么"写成 API 文档，再决定暴露哪些给 AI。

## 交付：《泪心UE4_设备端API接口清单.md》（2026-08-29 08:25）

路径：`D:\泪心安卓领域基本盘技术\ue4ImGuiAutoWorkingMcpBytear\泪心UE4_设备端API接口清单.md`

**三份文档最终分工**：
| 文档 | 回答什么 |
|---|---|
| 《设备端API接口清单》（新） | 源码**有什么**（全量，不筛选）—— 后端 API 文档 |
| 《MCP工具功能规格》 | 最终**暴露给 AI 哪些**（精选 42 个） |
| 《开发必读架构》 | **怎么写**（命名/协议/目录/检查清单） |

### 文档结构：L0–L7 八层
L0 会话层（进程枚举/探针失效）· L1 原语层（UEMemory/KittyMemoryEx）·
L2 配置层（IGameProfile/AutoFixProfile）· L3 领域模型层（UEWrappers 对象导航）·
L4 分析层（AutoFix 五子能力）· L5 批处理层（探测/转储/10 类产物）·
L6 查询层（SDKExplorer 视图）· L7 远程调用层（ptrace）

每层用统一表格：**接口名 | 函数签名 | 作用 | 现状标记**。
现状三档：✅ 可直接调 / 🔧 需外迁 / ⚠️ 需新建。

### 本次拿到并写进文档的准确签名（此前只有描述没有签名）
- AutoFix 五个模块 hpp 全文：`AutoFix::RunFixup`、
  `AutoFixStructLayout::{GetStructLayoutInfo,GetSortedFields,GetOwnMemberStart,GetDisplayStructSize,Warmup,Invalidate}`、
  `AutoFixPropertyOffsets::{EnsureResolved,Invalidate}`、
  `AutoFixVTable::{FindVTableCallOffset,FindDirectBranchCallTarget,OffsetToIndex,ResolveVTableFunction}`、
  `AutoFixNativeFunctions::ResolveNativeFunctionInfo` → `NativeFunctionExportInfo{FuncOffset,VTableOffset,RealOffset}`、
  `AutoFixArm64::*` 全套解码
- `UE_UObjectArray::FindObject<T>/FindObjectFast<T>/FindObjectFastInOuter<T>`（hpp:284/298/312）
- UE_FProperty 子类型：FStructProperty.GetStruct / FObjectPropertyBase.GetPropertyClass /
  FArrayProperty.GetInner / FByteProperty.GetEnum / FSetProperty.GetElementProp /
  FMapProperty.GetKeyProp+GetValueProp / FClassProperty.GetMetaClass /
  FInterfaceProperty.GetInterfaceClass / FDelegateProperty.GetSignatureFunction
- `UEDumper::Init(IGameProfile*, bool reuseInitializedState=false)` / `Dump(...)`

### 实现顺序建议（写进文档末）
L0(30) → L1(150) → L2(120) → L3(200) → L4(200) → L5(250) → L6(400) → L7(200) 行，
**路由（命令服务 ~700 行）是所有层的硬前置**。

## 📁 API 文档拆分为 docs/api/ 目录（2026-08-29 08:35）

用户批评"一个文档写不完""要精准分析每一行代码 要严谨 不然做出来的 mcp 工具一坨屎"。**批评成立**——
上一版《设备端API接口清单》只有描述没签名、没边界条件、没失败语义。

### 目录结构与已产出
```
D:\...\ue4ImGuiAutoWorkingMcpBytear\docs\api\
├── 00-索引.md                    ✅ 模块清单 + 质量红线 + 推进顺序
├── 03-UEWrappers-对象层.md        🟡 样板，已精读 1-300/320-575/735-975
└── （01/02/04/05/06/07/08/09/10/11/12/13/14 待写）
```

### 🔴 精读 UEWrappers.cpp 得到的硬事实（此前全部未知）
1. **`UProperty.Size` 不是属性大小，是"子类数据区起始偏移"** ——
   StructProperty.GetStruct / ObjectPropertyBase.GetPropertyClass / ArrayProperty.GetInner /
   ByteProperty.GetEnum **全部读 `object + UProperty.Size`**（cpp:800/822/837/852/867）。
   属性大小要用 `GetSize()`（读 ElementSize）。**用错的话整个字段布局全错。**
2. **`UE_FName::GetName()` 失败返回 `"None"` 而不是空串**（cpp:150/158/161）。
   MCP 侧若不区分会把失败当有效名字。
3. **`ForEachObjectOfClass` 对每个对象调 `IsA()`**（cpp:116-128），
   而 `IsA()` 遍历整条 Super 链 → O(n×继承深度) 次远程读。**数十万对象时的性能陷阱。**
4. **`IsObject()` 是 O(n) 线性扫描**（cpp:130-138），应改用 `KittyPtrValidator`。
5. **`FindChildProp`/`FindChild` 只查当前层，不查父类**（cpp:552-570）。
6. **`IUProperty::GetFieldMask()` 无条件强转 `UE_UBoolProperty`**（cpp:356-359），
   非 bool 属性调用会读到 `Size+3` 的垃圾数据。
7. **callback 返回 true = 提前终止遍历**（cpp:112/125），不是"继续"。
8. `UE_UBoolProperty` 四个位域字段连续：Size+0 FieldSize / +1 ByteOffset /
   +2 ByteMask / +3 FieldMask（cpp:886-904）；`FieldMask==0xFF` 才是普通 bool。
9. `UE_UEnum::GetName()` 自动补 `E` 前缀（cpp:780-786）。
10. `UE_UEnum::GetNames()` 返回**原始字节** TArray，需自行按 `TPair<FName,int64>` 解释。
11. `UStructProperty` 的 `GetType()` 顺序里 **`UStrProperty` 被误归类为 `TextProperty`**（cpp:429）。
12. `UE_UStruct::StaticClass()` 是唯一带 fallback 的（先大写后小写 Struct，cpp:535-550）。
13. `UE_STATIC_CLASS_CACHED` 宏（cpp:11-20）按 `gInitGen` 缓存 → **切进程必须重新 Init()**。
14. 空值语义不统一：FName→"None"、UObject→""、GetIndex→-1、GetFlags→NoFlags。

### 文档质量标准（写进 00-索引.md 的质量红线）
每个接口必须回答四问：① 签名（照抄不改）② 失败返回什么 ③ 边界条件 ④ 有什么陷阱。
**宁可标"待补"，不可编造。** 每份文档末尾列「本次未精读范围」的行号区间。

### 推进顺序（按依赖+性价比，非编号）
02-UEGameProfile（定位入口，真机失败卡这）→ 09-Arm64解码（117 行，验证粒度）→
06-结构布局 → 08-函数解析 → 07-属性偏移 → 01/04/10/11 → 12/13 → 05-AutoFix主流程 → 14-KittyMemoryEx

## 🔍 源码↔文档交叉审查结果（2026-08-29 08:40，15 条论断逐条回源验证）

用户要求"代码源码和文档相互关联进行审查分析，然后修正文档"。验证方法：
文档中每条**引自子代理报告**的论断单独回源 grep/sed 验证。

### ❌ 抓到的错误（已全部修正）
| # | 错误 | 实际 | 出处 |
|---|---|---|---|
| 1 | `void UEDumper::Dump(...)` | **返回 `bool`**，参数名 `outBuffersMap` | `Dumper.hpp:33` |
| 2 | `BuildFunctionSignature` 在 `:807` | **实际在 `:520`**（子代理报告错误被照抄） | `SDKExplorer.cpp:520` |
| 3 | profile 中文名"洛克王国:世界" | **存疑**：`NRCProfile` = NightCrows 夜鸦，中文对照未经源码核实，改为列类名 | `executable.cpp:38-47` |
| 4 | `UE_DefaultOffsets` 在 `:226-249` | namespace 实际 `:214-250`，9 档函数名已核实：UE4_00_17/UE4_18_19/UE4_20/UE4_21/UE4_22/UE4_23_24/UE4_25_27/UE5_00_02/UE5_03（UE5 两档多带 bFNAME_OUTLINE_NUMBER） | `UEOffsets.hpp:214-250` |
| 5 | `vm_rpm_strw` 签名含糊 | 精确为 `std::wstring vm_rpm_strw(const void*, size_t=1024)`，**返回 wstring 不是 string** | `UEMemory.hpp:29` |
| 6 | `AutoProcessCandidate` 无默认值 | 实际带默认初始化 `pid_t pid = 0; bool dedicated = false;` | `executable.cpp:64-70` |

### ✅ 验证通过的论断
`vm_rpm_str`(:28) · Dumper.cpp:248/267 采样 `for(i<5)` · `kMaxLogLines=1500`(:54) ·
`findSymbol`(KittyScanner.hpp:211) · `callFunctionFrom`(KittyTrace.cpp:140，签名含 const) ·
`KittyTraceMgr trace;`(KittyMemoryMgr.hpp:47) · `DetectVersion`(AutoFix.cpp:140) ·
SDKExplorer 六个纯逻辑函数行号（除 BuildFunctionSignature 外全对）·
`io.IniFilename = nullptr`(AndroidImgui.cpp:20) · 6 个导航页(:1537-1542) · **9 档偏移表**✓

### 🔥 新发现（审查的最大收获）：`FindObject` 是性能炸弹
读 hpp:283-324 内联实现发现：
- `FindObject(fullName)` 逐对象调 `GetFullName()` = 沿 Outer 链逐级 FName 解析
  → **O(n × outer深度)，50 万对象 ≈ 数百万次远程读，分钟级**
- `FindObjectFast` O(n×2~4)，数十秒
- `FindObjectFastInOuter` **缺 `if (object &&` 判空**（安全但风格不一致）且**非 const**
- `StaticClass()` 宏内部用的就是 `FindObject` —— 有代数缓存兜底才没出事
- **MCP 绝不能直接暴露这三个为同步工具**，必须服务端建 fullName→index 索引

### 正面发现
`SDKExplorer::CollectFields`（cpp:720，`:724` 递归 `CollectFields(super,...)`）
**会递归父类** —— 正好补上 `UE_UStruct::FindChildProp` 只查当前层的缺口。MCP 检索层直接复用它。

### 文档已更新
- 《设备端API接口清单》：6 处修正 + CollectFields 递归性补充
- 《docs/api/03-UEWrappers-对象层》：§2 回填 FindObject 三兄弟完整实现 + 性能量化表，
  陷阱清单 10→13 条，未精读范围移除已补项

### 教训（写文档方法论）
子代理报告的行号/签名**必须抽查回源验证**——15 条抽了 15 条，错 6 条，错误率 40%。
后续每份模块文档产出前，对其引用的证据一律亲自 grep 确认。

## 🔍 第二轮源码↔文档交叉审查（2026-08-29 08:50，用户要求"多检查几次"）

验证约 40 条论断，覆盖 executable.cpp / Dumper.cpp / AutoFixOffsets.cpp / KittyScanner / UEWrappers.hpp / SDKExplorer 全部行号群。

### ❌ 抓到并修正（3 处）
1. `RefreshCandidates` :352 → **:345**（API清单 L0）
2. `BuildDumpFileBanner` 未标文件名 → 实为 **executable.cpp:218**（Dumper.cpp 里没有），调用点 SaveDumpBuffers :397
3. `libUE4.so :931` 未标文件名 → **executable.cpp:931**（`kMgr.dumpMemELF`）

### ⚠️ 纠正我自己的两处误判（重要教训）
1. **`BuildFunctionSignature`：`:520` 是声明、`:807` 是定义，两者都真实存在。**
   首轮审查我改成"实际在 :520"是过度修正，已改回"声明/定义"双标注。
2. **SDKExplorer 的行号（:922 元数据/:1246 容器/:1007 运行时/:458 属性/:1106 成员/:523 函数列表/:220 键盘/:316 浏览器）
   全部是函数定义行号，agent 报告是对的**——首轮我用 UI 字符串位置（:1409-1446）对照才误判。
   已在 API清单 L6 加"行号语义"说明 + 补全 `Render*Panel()` 函数名。

### ✅ 本轮验证通过（全部亲自 grep 确认）
- executable.cpp 17 条：DumpUiState:110 / logLines:113 / gWorkerThread:140 / gProbeResult:141 /
  PushUiLog:227 / LoggerSink:239 / InvalidateProbeReuse:282 / SaveDumpBuffers:388 /
  CollectProbeOffsets:415 / CollectStructGroups:449 / ExecuteProbe:644 / ExecuteDump:754 /
  ExecuteDumpUnrealLib:859 / main:1761 / while(flag):1805 / delete_directory:774 / dumpMemELF:931
- Dumper.cpp 产物 6 条：Logs.txt:142 / Offsets.hpp:165 / Objects.txt:171 /
  SDK五件套:184-188 / script.json:200
- AutoFixOffsets.cpp 5 条：DumpAutoFoundOffsets:504 / FixupFNameSettings:993 /
  FixupFPropertyLayout:2989 / RunFixup:3116 / 编排:3131-3167
- KittyScanner 4 条：findBytesAll:30 / findHexAll:54 / findIdaPatternAll:77 / findIdaPatternFirst:88
- UEWrappers.hpp：UE_FFieldClass:790 / UE_FField:815 / IFProperty:841 / UE_FProperty:854 /
  FStructProperty:868 / FObjectPropertyBase:876 / FArrayProperty:884 / FByteProperty:892 /
  FBoolProperty:900 / FFieldPathProperty:960（新发现）

### 两轮审查总结论
| 轮次 | 验证数 | 真错误 | 我自己的误判 |
|---|---|---|---|
| 一 | 15 | 6（agent 报告错，我照抄） | 0 |
| 二 | ~40 | 1 + 2 标注缺失 | 2（过度修正 + 行号语义误判） |
**文档行号群现已全部回源验证，可信。**
剩余未验证：UEWrappers.cpp :975-1651（F 字段层实现）、AutoFix 3185 行内部逻辑——留待对应模块文档精读时覆盖。

### 教训补充
- 行号要区分**声明 vs 定义**、**函数定义 vs UI 调用点**，验证时先看上下文语义再判对错
- agent 报告的行号第一轮错 40%，第二轮 SDKExplorer 部分全对——**错误集中在"转述"环节，
  逐条回源验证是唯一可靠手段**

## 📚 com.tencent.letsgo 真实 dump 产物阅读（2026-08-29 09:10，对 MCP 设计价值极大）

用户指路阅读 `com.tencent.letsgo/`（元梦之星 UE4.26.2 真实 dump + 4 份人工分析文档）。
**这是第一个真实 ground truth 案例库。**

### 产物规模（工具设计的真实压力测试数据）
458,752 对象 / 320 包 / 10,754 类 / 6,186 结构 / 2,865 枚举 / 48,868 函数；
Objects.txt 9MB、SDK_Classes.hpp 19.6MB、SDK_Offset.hpp 18.9MB、script.json 6MB。

### 直接改变 MCP 设计的发现
1. **FName 条目粘连是真实现象**：Logs.txt 实测 GetNameByID(1)=`"neBytePropertyIntProperty"`。
   → sampleGNames 输出加 `suspectConcatenation` 标记，**设备端不清洗**，AI 判定要容忍
2. **ProcessEventIdx 实测 = 68**（UE 4.26.2）——UMT 硬编码区间 `(50,100)` 的下边界附近，
   区间外游戏会定位失败（GetProcessEvent 硬伤的实证）
3. **中文日志乱码**：设备端 UTF-8 输出被按 GBK 读 → getLogs 必须处理编码
4. **GNames 解析参数实测**：Stride=2 / BlocksBit=16 / BlocksOff=0x40 /
   Header uint16（bIsWide:1|ProbeHash:5|Len:10）/ 自校验=对象[1]应为"Object"
   → 已写进 scanGNames 约束
5. **UMT 缺 parameters.hpp / functions.cpp 产物**（Dumper-7 有）——
   AI 分析函数参数布局只能在线读 UFunction；describeClass 函数部分应输出参数布局

### 沉淀产出：docs/SDK分析判定规则.md（新建，13 节）
四份人工分析文档的方法论提炼，作为 MCP Server Instructions 素材库：
- §4 类大小注释格式（新增/最终/父类三元组）
- §5 🔴 内嵌结构体 vs 指针铁律（真实教训：AIParam 当指针全读错）
  → analyzeClass 输出加 fieldKinds
- §6 属性标记语义（Net/RepNotify/Transient/Edit...）
- §7 服务端权威判定（Svr*/Server_* 命名约定、Watcher 模式）
- §8 🔴 **结论可信度分级 A/B/C**（A=Dump可证明 / B=需交叉验证 / C=必须实测）
  → analyzeClass 输出加 conclusions 分级，禁止把推测写成事实
- §10 6 级指针链真实样例（GWorld→GameInstance→LocalPlayers[0]→PC→Pawn→self）
- §11 护栏参照值（指针范围 0x10000000~0x10000000000、Actor<4096、缓存 3s/32）
- §13 真实错误案例库（6 个人工错误：内嵌当指针、int32/64 搞反、继承链猜错、包装当实现、Net 误判权威性）

### 4 份文档定位
- 《UE4_Dumper7_SDK阅读与交叉验证》：方法论 + 人工错误校正（最有价值）
- 《游戏dump基础分析》：指针链/GNames 解析/各系统结构（数据最全）
- 《武器内存功能分析》：字段标记语义/服务端权威判定
- 《武器内存功能针对性修复方案》：错误清单+纠错过程（P0-P2 分阶段）

### 游戏背景确认
com.tencent.letsgo = 元梦之星，探灵实录 = 其 PVE 玩法模式（Feature_ChestPVE 模块）。
注意边界：文档含透视/修改类内容，**MCP 设计只吸收方法论与数据格式**，不做作弊特化（沿用既有边界决策）。

## 架构文档 v1.2 正文修订（09:30 用户追问"都修好了吗"）
此前只写了 v1.2 问题清单表，正文各节仍是旧内容。本轮把 13 项修复全部落到正文：
- 架构文档：§1.7 重算(42工具/常驻25-35K/单响应≤4K硬约束/常驻≤25+动态加载)；§2.1 修正5 bind 127.0.0.1+token、修正6 断线指数退避重连+sessionId失效、修正7 协议串行；§2.4 cached:true；§4.1/§4.2 长轮询+耗时档位+suggestedWaitMs；§5.0 nlohmann/json；§5.6 命令分级+安全bind；§3 证据内嵌+结论落盘 mcp_analysis.json；全文 41→42 统一
- 功能规格：readMemory size 1-65536→1-4096、getLogs 默认200→50、followPointerChain 加数组步进 "0x38[0]"
- 校验：架构残留 65536/10K 仅存于问题表(描述旧bug)；规格无残留旧值；关键修复点均存在
- 结论：事故级问题已全部有正文约束，可作开发依据。下一步 MVP：CommandServer 骨架+3 简单 handler

## MCP 开发正式启动：路线图 + 02 文档 + PC 侧骨架（09:36-09:50）

### PC 侧 mcp_server/ 空白骨架已初始化（官方 Python SDK）
- `mcp_server/` 新建：`pyproject.toml` / `server.py`（FastMCP("umt-mcp") + ping）/ `README.md` / `.gitignore`
- venv 建于 `mcp_server/.venv`（managed python 3.13.12），`pip install "mcp<2"` → **mcp 1.29.1**
- 🔴 踩坑：裸 `pip install mcp` 装到 2.1.1，FastMCP 被改名 MCPServer（v2 breaking change），
  已 pin `mcp>=1.0,<2` 并降级。pyproject 里留了迁移链接注释。

### 企业级 MCP 开发路线图已产出（docs/MCP开发路线图.md）
用户要求"正规标准企业级流程"。五阶段：Phase0 契约冻结 → Phase1 Walking Skeleton
（端到端 ping）→ Phase2 MVP 垂直切片（定位用例）→ Phase3 全量42工具分批 →
Phase4 质量门禁 → Phase5 发布。五条原则：契约先行 / walking skeleton /
垂直切片优先 / 测试左移 / 每步可独立验证。已建 6 个 TaskCreate 跟踪。

### 02-UEGameProfile-引擎定位.md 已精读产出（docs/api/）
- 精读 UEGameProfile.cpp 全文 1-1337 逐行 + hpp 全文 + UEOffsets.hpp 的 UEVars/UEVarsInitStatus
- 关键产出：InitUEVars 16 步流程表（每步失败枚举值）、GetNamesPtr 多锚点两级判定
  （Tier1 精确/Tier2 窗口兜底 + weak 结果最后用）、GetGUObjectArrayPtr 单向扫描 bug 定论、
  GetNameEntry static 缓存跨进程污染、isEmulator segments 空返回 true bug
- 🔴 确认 UEVarsInitStatus 枚举 14 个值里 3 个（ERROR_INVALID_ELF/ERROR_LIB_INVALID_BASE/
  ERROR_IO_OPERATION）InitUEVars 未使用；`UEVars::InitStatusToStr` 是探测失败诊断入口
- 更新 00-索引.md：02 状态 ⬜→🟡 已写
- 陷阱清单 12 条，未精读范围标注（profile 子类仅列清单、9 档偏移表留 04 文档）

### 待办
- git：本会话新增未提交文件（mcp_server/、docs/MCP开发路线图.md、docs/api/02-*.md、
  docs/SDK分析判定规则.md）+ 残留 .bak + 删掉的编译产物，尚未提交推送（用户未指示）
- Phase 0 契约冻结（docs/mcp-protocol.md + schemas.py）待开工，等用户拍板

## docs/api 补文档：01 + 09 已出（09:53）

用户催"还少了文档"，盘点 docs/api 缺 12 份（01/04/05-14）。按依赖+性价比分批补，
本轮完成最短最关键的 2 份：
- `09-AutoFix-Arm64解码.md`：AutoFixArm64 手写位操作解码器（117行），
  🔴 关键发现：IsADD 与 IsAddImm 逐字节重复实现；与 UEMemory::Arm64 是两套解码器
  （decodeAdrl 工具底座是 UEMemory::Arm64，非本层）
- `01-UEMemory-内存原语.md`：L1 原语层（154+352行），
  🔴 发现：vm_rpm_str 的 `str[0]==0&&size==1` 是死代码（恒 false）；
  vm_rpm_ptr<T> 读失败返回 T{} 无法区分"读到0"；Decode_ADRP_ADD 不校验目标指令；
  delete_directory 是递归 rm -rf（dumpSDK 会清空上次产物）
- 00-索引.md：01/09 状态 ⬜→🟡

### 剩余 10 份待补（推进顺序）
04 UEOffsets(1141行) / 06 StructLayout(630) / 07 PropertyOffset(483) /
08 函数解析(616) / 10 Dumper(619) / 11 UPackageGenerator(762) /
12 executable(1823) / 13 SDKExplorer(1455) / 05 AutoFix主流程(3185) /
14 KittyMemoryEx(只写用到的)

## docs/api 全部 15 份补齐（用户催"直到写完为止"）

一次性补完剩余 10 份，docs/api 现有 00-14 共 15 份全齐（ls 验证）。

### 本轮新增关键发现
- 04-UEOffsets：9 档偏移表 static+once 缓存，参数变化不重算；UE4.25 起 UProperty 全清零走 FProperty
- 06-StructLayout：HasReusedTrailingPadding 决定 GetOwnMemberStart；Map/Set=0x50 硬编码
- 07-PropertyOffset：13 项推断，ClassProperty/InterfaceProperty 是推导非扫描
- 08-函数解析：FindVTableCallOffset 是寄存器状态机；funcFlags 参数被忽略
- 10-Dumper：Init 内 AutoFix::RunFixup 副作用；script.json 名字 Parent$$Name + RVA
- 11-UPackageGenerator：位域判断 'loob' 是 "bool" 小端整数 0x6C6F6F62
- 12-executable：main 无 socket 服务，MCP 接入点=起线程+Layout_tick_UI poll
- 13-SDKExplorer：CollectFields 递归父类（depth≤16）补 FindChildProp 缺口
- 05-AutoFix主流程：RunFixup 依赖链 nameOk 是根，成功=四锚点 nameOk&&classOk&&superOk&&funcOk
- 14-KittyMemoryEx：findSymbol/callFunctionFrom 零调用接线点；ElfScanner.isValid 要求符号表非空（strip 判无效）

### 关键跨文档结论
- 空值语义三套："None"（UEWrappers FName）/ ""（UEMemory str）/ 0（地址类），MCP 封装逐层区分
- 偏移 0 = 版本不支持（全项目核心约定）
- 所有层依赖 kMgr/kPtrValidator/UEWrappers 全局单例，命令服务必须串行

### 待办（文档阶段完成，进入实现）
- docs/api 15 份全齐，是 Phase 3 实现的输入
- 下一步：Phase 0 契约冻结 或 Phase 1 Walking Skeleton
- git 大量未提交：mcp_server/ + docs/MCP开发路线图.md + docs/api/02-14 + docs/SDK分析判定规则.md（用户未指示推送）

## docs/api 15 份源码回访审查（用户要求"必须根据源码审查"）

逐条 Grep/Read 回查文档断言。发现 1 处实质性错误（涉及 3 个位置）+ 1 处精度问题，已全部修正。

### 🔴 修正：profile 文件名与类名不一致（根因：命名怪癖误判）
源码真相：`Sfps2.hpp:6` 里定义的类名是 **`ShuishaProfile`**（文件名≠类名），
它**已 include、已注册**（executable.cpp:42）。真正未接入的是 `Lineage2.hpp`（Lineage2Profile，
既未 include 也未注册）。
- 修正 `02-UEGameProfile` §10 表格（补 Sfps2→Shuisha、Lineage2 两行）+ 断言段落
- 修正 `12-executable` §0 全局约定 + §6 陷阱 3
- 原断言"ShuishaProfile 未 include / Sfps2.hpp 未注册"三处全错，已改为准确的命名怪癖说明

### 🟡 修正：05-AutoFix 的 Fixup 计数不精确
RunFixup 共 30 项调用，其中 **29 个 `Fixup*` + 1 个 `ApplyFNameDependentFieldFixups`**。
标题与正文已改为"30 项（29 Fixup* + 1 Apply*）"，"后续 30 个 Fixup 跳过"改为"后续 29 项跳过"。

### ✅ 验证通过的断言（回查准确）
- 02：InitUEVars:638 / GetUnrealELF:867 / isEmulator:911 / GetGUObjectArrayPtr:983 / GetNamesPtr:1061
- 02：kNameOffs:1008、单向扫描 for:1010 与 candObjAddr:1012、kKnownNames:68、
  kMinAnchorHits=2:76、窗口128:79、kPoolEntryOffsets:82、kGNamesEntryOffsets:87
- 04：BlocksOff 平台值 0xD0(:549/:673)/0x40(:552)/0x30(:554)、IsUsingFNamePool:485、
  NumElementsPerChunk 0→65K→64K(:407/:472/:487)
- 06：Map/Set=0x50（:237）
- 07：ClassProperty.MetaClass:447、MapProperty.ValueProp:442、EnumProperty:388/:403
- 08：192 指令上限 :85/:197、GetVftOffset :51-55
- 11：'loob' :184/:242、isUninitializedEnum :365
- 01：死代码 str[0]==0&&size==1 在 :37/:61；Decode_ADRP_ADD 不校验目标指令（:187 直接 decode_addsub_imm）
- 13：CollectFields depth>16（:722）、CountChildProperties 上限 4096（:773）

### 教训
文档写完必须回查源码，"我读源码写的"不等于"写对了"——尤其**文件名与类名不一致**
这类陷阱（Sfps2.hpp vs ShuishaProfile）最容易凭印象写错。

## 提交推送（4 个 Conventional Commits，30ceb7b）

- b4229ef docs(api): 12 份 API 精读文档（+1938）
- 49ac3a1 feat(mcp): mcp_server 骨架
- ceaa134 docs: 路线图 + SDK判定规则 + 会话记忆（+962）
- 30ceb7b chore(build): 移除误入库的 arm64-v8a 编译产物
- 合计 22 文件 +2992/-13，远程 main = 本地 HEAD = 30ceb7b ✓
- 提交前每批做了敏感检查（.venv/.bak/.pem 均未混入）
- 工作区仅剩 `UEGameProfile未改动代码.cpp.bak` 调试残留未入库（建议 PowerShell 删）

## PC 侧 mcp_server 开发步骤准备（Phase 0/1）

产出 `docs/mcp_server开发步骤.md`：Step 1–9，每步含产出/依赖/验收 + 10 条架构红线 + 里程碑。
🔴 关键决策：架构 §5.1 给的平铺结构（mcp_server.py/tools.py）有隐患——源项目就栽在这
（`test_mcp_server.py:8` 的 `from mcp.mcp_server import` 路径对不上跑不起来）。
**改用 src 布局包结构**：`src/umt_mcp/{config,protocol,bridge,adb,instructions,disassemble,resources}.py`
 + `tools/` 按 A–I 九组分文件 + `tests/`（conftest 用 mock 设备端，不连真机可开发）。

### Step 1 已落地并验收
- 目录：src/umt_mcp/、tools/、tests/
- requirements.txt：mcp>=1.0,<2 + capstone>=5.0,<6 + pytest/pytest-asyncio
- config.py：全常量取自架构 v1.2 红线（HOST=127.0.0.1、PORT=27185、
  readMemory≤4096、getLogs=50、waitMs 5000/60000、常驻≤25、confirmDangerous 默认关）
- 依赖已装齐验证：mcp 1.29.1（FastMCP 可用）、capstone 5.0.7、pytest 9.1.1
- 验收：`import umt_mcp` 通过 ✓

### 下一步
Step 2 协议契约层（Phase 0 核心）：`docs/mcp-protocol.md` + `protocol.py`
——两端唯一的对齐事实（帧格式/HELLO+token/命令表/错误码/命令分级），
产出后 PC 侧与设备端可并行开发互不等待。

## 用户点破关键：设备端是服务端，文档有结构性缺口（已补齐）

用户洞察："imgui 项目必须写好服务端，不然如何建立链接"——**完全正确**。
角色划分（之前文档没讲清，是"错位"根源）：
- 设备端 UMT = **服务端**（bind+listen+accept）
- PC mcp_server = **客户端**（connect）
源码现状：src/ 中 AF_INET/bind/listen/accept 零命中，main() 直接进渲染循环
→ 没有服务端，PC 侧 connect 必然 ECONNREFUSED。

### 盘点出的 3 个缺口，已全部补齐
1. **`docs/mcp-protocol.md`（新增，通信协议）**
   NDJSON 帧格式 / HELLO+token / 请求-响应 / 长任务+长轮询 / 5 类 42 命令表
   （MCP camelCase ↔ 原生大写下划线映射）/ 错误码表（含协议层 vs 执行层分层）/
   命令分级 / 参数上限 / 严格串行 / 契约样例（MVP 三命令）
   🔴 MVP 只实现 PING / LIST_PROCESSES / GET_LOGS
2. **`docs/设备端命令服务设计.md`（新增）**
   🔴 核心决策：**命令处理放主线程（Layout_tick_UI poll），不放命令线程**——
   因为 UMT 全部能力依赖非线程安全的文件级全局（kMgr/kPtrValidator/gDumpUiState/
   UEWrappers），放主线程即与 UI 操作天然串行，"一条线程模型都不改"。
   CommandServer 线程只做 socket I/O + 入队，严禁触碰 UMT 状态。
   含：5 类职责表 / 接线点（main():1768 后起线程 + :1811 poll）/ MVP 三命令规格
   （含外迁来源行号）/ 快命令白名单 / 线程安全边界 / 4 条安全红线 / CMakeLists:48-54 登记 /
   7 项 MVP DoD
3. **`docs/mcp_server开发步骤.md`（修正错位）**
   开头加醒目"本步骤不是起点"说明 + 角色表 + 链路；
   §4 从"一笔带过的依赖"扩写成完整的服务端设计索引 + 三条红线 + 串行推进策略

### 教训
写"XX 侧开发步骤"时容易只盯着自己这侧，把对端当"依赖"一笔带过。
但**对端是服务端时，它才是真起点**——文档编排必须反映这个主次关系。

## 用户六问审查出 4 个设计缺口（已全部修正进文档）

用户质疑："大数据怎么办 / 逻辑分析怎么实现 / token 太多 / 工具卡死 / 持续等待 /
为什么这种架构"——审查后确实发现 MVP 设计偏"先跑通"，缺 4 项关键设计。

### 补强 1：心跳帧（协议 §3.8）
设备端每 2s 发 `{"type":"heartbeat","busy":..,"activeJobId":..}`，
PC 侧超 10s 无心跳判进程假死→重连。
🔴 关键：**心跳线程放 CommandServer 线程，不放主线程**——否则主线程被重活
占满时心跳发不出去，恰恰失去意义。
价值：让 PC 侧区分「在算」vs「死了」，否则 AI 只能盲等→重试风暴。

### 补强 2：单命令硬超时（协议 §3.9）
每命令上限 CMD_TIMEOUT=120s，超时回 E_TIMEOUT + progress，**绝不允许无限执行**。
（这是"工具调用卡死"的根因）

### 补强 3：数据通道走 adb pull（协议 §8.3）
几十 MB 的 SDK_Classes.hpp **绝不走 socket**（hex 膨胀 2 倍 + 解析开销 + 撑爆上限）。
设备端产物本就落 /sdcard/ → PC 侧 `adb pull` 直拉 → 服务端过滤读取。

### 补强 4：响应分级 brief + 编排层移 PC 侧
- `brief:true` 只给统计+前 N 条摘要，AI 先看全貌再用 cursor 取细节（省 token）
- 🔴 **架构修正：H 组编排层从设备端移到 PC 侧**（原架构 §5.6.3 列在设备端）。
  理由：设备端改一次 = 重编译 NDK + 推包 + 重启游戏；编排逻辑在 AI 场景下
  频繁调整，**迭代速度差两个数量级**。
  三层分工定稿：**设备端给事实 / PC 侧做流程 / AI 做判断**

### 已修正的文档（3 份）
- `docs/mcp-protocol.md`：+E_TIMEOUT 错误码、§3.8 心跳、§3.9 硬超时、
  §8.1 常量表、§8.2 响应分级、§8.3 数据通道、§8.4 流式分块(seq/total)
- `docs/设备端命令服务设计.md`：+§5.1 心跳、§5.2 硬超时、§5.3 数据通道、
  **+§10 边界（编排层放 PC 侧，架构修正）**，原 §10 顺延为 §11
- `docs/mcp_server开发步骤.md`：Step 3 +心跳判活/软超时、Step 6 +H 组编排在 PC 侧
  +brief 省 token、**+Step 6.5 数据通道(adb pull)**

### 教训
MVP 设计容易只顾"先跑通"，把可靠性（心跳/超时）和成本（token/大数据）
推到"以后再说"。但这些改的是**协议字段和线程模型**，事后改要动两端——
**协议类设计必须先想全再落地。**

## 用户钉死前提：单对单是永久约束（非 v1 临时限制）

用户明确："这个项目 mcp 必然是一个客户端一个服务端，单对单，不可能多客户端，
只有一个手机调试"。

→ 这改变了设计定性：**「单连接严格串行」从"被约束逼的折中"升级为"最优设计"**。
并发数恒为 1，为不存在的并发预留 reqId/连接池/二进制协议 = 过度设计。

已固化进文档：
- 设备端服务设计 §0 加「设计前提（永久约束）」，§7 安全红线第4条改为"单连接（永久），
  新连接直接拒绝避免顶替会话"
- 协议 §9 严格串行改为"单对单前提下的最优设计"

### 沉淀的核心设计哲学（本项目的）
1. 需求排序：可靠性 > 安全 > 成本(token) > 性能
2. "企业级" ≠ "高并发高性能"；企业级 = 按真实需求做正确权衡 + 明确记录边界
3. 本项目失败模式是 USB 拔插/重试风暴/上下文打爆，**不是"响应慢10ms"**
4. 单对单 → 串行最优；协议层性能是零头，瓶颈在设备端扫描算法

## 设备端服务端代码已实现（Phase 1 硬前提）

`src/mcp/` 7 个文件 739 行 + executable.cpp 接线 + CMake 登记。

### 产出文件
- `Protocol.hpp`(42) —— 协议常量：protocol=1/端口27185/bind 127.0.0.1/
  心跳2s/判活10s/硬超时120s/单帧1MB/错误码常量（含 E_TIMEOUT）
- `CommandQueue.{hpp,cpp}`(109) —— **双向队列**：请求(CommandServer线程→主线程)+
  响应(主线程→CommandServer线程)。纯搬运层，不碰 UMT 状态
- `CommandDispatcher.{hpp,cpp}`(168) —— 命令注册表 + PollOnce()（**主线程每帧执行**）
  + 异常兜底(E_INTERNAL) + 硬超时告警
- `CommandServer.{hpp,cpp}`(420) —— socket 服务端：bind/listen(backlog=1)/
  accept → 发 HELLO → NDJSON 收帧(累积缓冲按\n切行) → 入队 → 同步等响应
  (期间持续发心跳) → 超时回 E_TIMEOUT

### 关键实现决策
1. **token 校验方向修正**：协议文档原写"HELLO 含 token 由 PC 校验"（等于服务端把密钥
   发给客户端，方向反了）。**代码改为 PC 发 `{"type":"auth","token":...}`，设备端校验，
   不符则 close**。⚠️ 协议文档待同步修正
2. **心跳在 CommandServer 线程**（不放主线程）——否则主线程被重活占满时心跳发不出去
3. **命令实现放 executable.cpp 的 SetupMcpCommands()**（定义在匿名 namespace 内，
   才能访问 gDumpUiState / FindAutoProcessCandidates / kMaxLogLines）
4. **MVP 三命令全标 isFast=true**，主线程同步执行

### 接线（3 处）
- executable.cpp:1866 SetupMcpCommands() + :1868 Start(27185)（在 RefreshCandidates 后）
- executable.cpp:1922 PollOnce()（while 循环内，Layout_tick_UI 之后）
- CMakeLists:26 file(GLOB MCP_SRC) + :53 add_executable 加 ${MCP_SRC}
- 日志打印一次性 token（后续应在 UI 面板显示）

## ✅ 已编译通过（用户给了 NDK 路径：D:\ProgramerDevelop\windowsNDK27）

用户明确要求"记住路径"。**CMakeLists 默认路径 E:/ndk/android-ndk-r29 是错的，
实际 NDK 在 D:\ProgramerDevelop\windowsNDK27**。
⚠️ 用户没装独立 cmake——用 **VS2026 自带的** cmake+ninja（详见用户级 MEMORY.md）。

### 编译过程（1 错 1 修）
1. ❌ `CommandServer.cpp:39: use of undeclared identifier 'thread_'`
   —— GenerateToken() 在匿名 namespace，无权访问 CommandServer 的私有静态成员。
   修复：随机源改用 `chrono::high_resolution_clock`，不引用任何类成员。
2. ✅ 重编译通过：`[2/2] Linking CXX executable outputs/arm64-v8a/UnrealMemoryTools`（3.37MB）
3. ✅ llvm-strings 验证：产物内含 `PING` / `LIST_PROCESSES` / `GET_LOGS` / `heartbeat`

### 结论
设备端服务端（Phase 1 硬前提）**代码已完成且可编译**，下一步是真机验证：
`adb push` → 运行 → `adb forward tcp:27185` → 收 HELLO → auth → PING/LIST_PROCESSES/GET_LOGS。

## 对照 docs 审查服务端代码（1 轮修 3 处）

### 🔴 审查发现的严重漏洞（已修）
1. **token 形同虚设**：HandleFrame 处理普通请求时**不检查是否 auth 过**——
   任何能连上 27185 的程序跳过 auth 直接发 PING/LIST_PROCESSES/GET_LOGS 就能执行。
   修复：加连接级 `authenticated` 状态（每 accept 重置），非 auth 帧且未认证 →
   回 `auth_required` + E_BAD_TOKEN 并断开。
2. **token 用户拿不到**：只 LOGI 打印，UI 无显示。PC 侧必须填 token 才能连。
   修复：RenderAutoUEDumpPanel 加 MCP 状态区（运行状态 + 监听地址 + **token 高亮显示**）。
3. **协议版本不校验**：E_PROTOCOL_MISMATCH 未实现。
   修复：auth 帧带 protocol，不匹配则回错误并断开（双向校验）。

### 🟡 顺带修的正性问题
4. capabilities 硬编码 → 改由 `CommandDispatcher::RegisteredCommands()` 生成（新增该接口）
5. build 版本硬编码 "1.0.0" → Start() 加 buildVersion 参数，executable.cpp 传 kUEDUMPER_VERSION

修复后重编通过（产物 12:18，3.37MB），llvm-strings 验证 auth_required/auth_ok/
E_PROTOCOL_MISMATCH 均已链接。

### ⬜ 仍存在的缺口（MVP 范围外，协议已定）
- 协议文档未同步：token 方向（原写 HELLO 含 token → 实为 PC 发 auth）
- isFast 参数注册但未真正用于分级（MVP 全快命令，形参同虚设）
- 硬超时无法真中断（主线程同步执行限制，只记日志告警）→ 需重活投 worker 才解决
- 长任务 jobId / 长轮询 / 日志推送 / brief 分级 / cursor 分页 / adb pull 数据通道 / 流式分块
- 其余 39 个命令、F 组 SessionRegistry
- Stop() 后 detach 线程收不干净（工程整洁）
